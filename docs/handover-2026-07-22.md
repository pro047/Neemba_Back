# 인수인계 문서 (2026-07-22)

> 기존 handover-2026-07-19.md를 대체하는 단일 문서 (구버전은 git 히스토리에 있음).
> 완료 항목 세부는 git log(PR #45~#49, Neemba_Front #1)가 진실.

---

## 1. 작업 규칙

- **브랜치**: `main - develop - feature`. 모든 PR은 develop으로, develop→main은 release PR로만(머지 = 자동 배포). feature 브랜치는 머지 후 삭제. **mvp(Neemba_Front)도 동일 구조**(2026-07-20 세팅, 기본 브랜치 develop).
- **TDD**: red → 수정 → green + 전체 스위트 + `tsc --noEmit`/`py_compile` → 한국어 conventional commit → PR → 머지.
- **테스트**: Node `cd services/node && npm test && npx tsc --noEmit` (**75 passed**). Python `cd services/python && uv run --extra dev pytest tests/ -q` (**104 passed**).
- **gh 계정**: `pro047`. auto-merge 꺼짐 — 체크 통과 대기 후 수동 머지. 로컬 origin은 https(gh 인증 push).
- **branch protection**: develop/main에 `node-test`·`python-test` 필수 체크.
- **AWS 변경은 `infra/terraform/`에서 코드→plan→apply로만.** state=`s3://hymn-tfstate/neemba/terraform.tfstate`. (SG·EC2·deploy IAM은 테라폼 미흡수 — 백로그)

## 2. 현재 가동 중인 운영 체계 (전부 배포·검증 완료)

| 체계 | 구현 | 비고 |
|---|---|---|
| CI 테스트 게이트 | deploy.yml `test` 잡 + ci.yml | 실패 시 배포 미시작 + 머지 차단 |
| 인증서 D-14 감시 | cert-watch.yml — 매일 06:00 KST | 2026-10-10 만료 |
| /health 외부 감시 | health-watch.yml — 5분 cron + 디스코드 | 서버 밖 시점 계층 — 유지 |
| CPU 크레딧 누수 감시 | cpu-credit-watch.yml — 매일 06:00 KST | 임계 60크레딧/24h |
| Postgres 백업 | pg-backup 사이드카(S3, 90일) | 복원 리허설 완료 |
| RTMP 안정화 | ffmpeg 백오프·on_publish 즉시 재시작·워커 1 고정 | 인증은 **아직 꺼짐**(§4-1) |
| ws 세션 복구 | 끊김 즉시 인지 + pending 큐 + 재접속 방류 | 7/22 예배에서 실전 PASS(§3) |
| **publish 재시도 버퍼** | **NATS 순단 시 span 보존→순서대로 재발행 (TTL 60s·캡 500) + NATS 무한 재연결 + 스트림 max_age 600s 정합** | 2026-07-22 배포(§3). 재현: `services/node/scripts/repro_publish_retry.ts` |
| **파이프라인 알림 사이드카** | **monitor 컨테이너 — /metrics 폴링(60s), 심장박동(활성 중 공백 180s)·에러 시그니처 9종·상태전이 dedup(발생1+복구1+30분 리마인더)·디스코드** | 2026-07-22 배포·prod 실전송 확인(§3). 웹훅은 ENV_PROD의 `DISCORD_WEBHOOK_URL` |
| AWS 테라폼 | 백업 자원 흡수, drift 0 | EC2·deploy IAM·SG 미흡수(백로그) |

## 3. 이번 기간(2026-07-20~22) 완료

| 작업 | PR | 요지 |
|---|---|---|
| §4-3 mvp 정리 | Front #1 | feature/mic WIP → 의미 단위 3커밋(ws 재시도 12회 / 서버 TTS audioplayers / 스와힐리+TTS 폴백) → develop 머지. main-develop 구조·기본 브랜치 세팅. 잔여: mvp CI+protection(백로그), mic 실동작 검증 |
| §4-4 backpressure | #46·#47 | 재시도 버퍼(순서 보존이 핵심 — consumer seq 워터마크가 추월 span을 duplicate 드롭) + 무한 재연결(기존은 20초 후 영구 사망) + max_age 정합. dev에서 NATS 18초 강제 순단 → 유실 0/25 라이브 PASS |
| 수요예배(7/22) §4-1 실전 검증 | — | 전 항목 PASS: 첫 번역 7초, keepalive ping/pong 정상, Stt paused·ffmpeg 정체 0건, 끊김 0건, OBS 실스트림 기록(h264 1280×720 + AAC 48kHz). `auth disabled` 로그로 §4-2 미설정 재확인 |
| 모니터링 강화 | #48 | 도메인 메트릭(python 5종·node 5종, /metrics는 컨테이너 내부 전용) + monitor 사이드카. 규칙 엔진은 순수 함수로 CI 편입. dev 라이브 배터리에서 실버그 3건 발견·수정(scrape 과민→2연속, 일일 틱 소모 레이스, urllib UA 403). **전 규칙 9종 실제 디스코드 전송 성공**(성공 기준) |
| release 배포 | #49 | prod 가동 확인: 전 컨테이너 healthy, `max_age RECONCILED 259200→600s`(prod는 3일이었음), monitor가 prod에서 첫 알림 실전송. ENV_PROD에 웹훅 반영(사용자) |
| SSH 접근 복구 | — | SG의 22포트 허용 IP 변경(사용자 직접, IP 재할당 대응) |

## 4. 다음 작업 (우선순위 순)

### 4-1. RTMP 인증 활성화 — **사용자 작업** (현재 아무나 publish 가능, monitor가 매일 리마인더 알림)
1. OBS 스트림 키: `translation` → `translation?key=<비밀값>`
2. 로컬 `.env.prod`에 `RTMP_PUBLISH_KEY=<비밀값>` 추가 → `gh secret set ENV_PROD -R pro047/Neemba_Back < .env.prod`
3. 다음 배포부터 강제. 켠 뒤 "RTMP 인증 꺼짐" 일일 알림이 사라지는 게 완료 신호.

### 4-2. 다음 예배: 재시도 버퍼·모니터링 첫 실전 관찰
알림 없음 = 정상. 순단 발생 시 심장박동·복구 알림 도착과 `publish buffer: retrying` 후 자막 유실 없음 확인.

### 4-3. mvp 후속
mic 탭 실동작(서버 TTS `/api/mic/tts` 백엔드 구현 여부 확인 포함), mvp CI(flutter analyze)+branch protection.

### 4-4. /ws 인증 + 다중 청중 허브 재설계 (구 §4-5)
현 WebSocketHub는 단일 슬롯(동시 1클라) — 다중 청중 로드맵에 따라 per-connection 구조로 재설계. Flutter 수정 동반이라 mvp 정리 후 한 세션에서. **주의: hub_active_session 등 메트릭·심장박동 게이트도 per-connection 재설계에 맞춰 갱신 필요.**

### 4-5. IAM 최소권한 분리 (구 §4-6, 상향 권고)
`ljs`(AdministratorAccess) 단일 키가 로컬+GHA 양쪽 사용 중. 읽기 전용+상승 구조로, 테라폼으로. SG·EC2·deploy IAM 테라폼 흡수와 묶어서.

### 4-6. 백로그
디스크 사용률 감시(호스트 / ro 마운트 트레이드오프 결정 필요) / 드롭 카운터 외 메트릭의 Grafana 대시보드(알림 체계 안착 후) / GHA 감시 3종 dedup / DeepL 클라이언트 재사용·비동기화 / VAD rotation / 찬송 모드 / Makefile·ESLint / GHA Node20 버전업 / terraform S3 native lock / `imports.tf` 삭제 / postgres 루프백 공개 / main.py pong send 게이트 밖(구 REV-5 H3).

## 5. 미해결·주의사항 (이월+신규)

- **서버 실 IP = 13.125.26.93** (EIP, i-067ebdc3337b14ff9, t3.medium). SSH 키 `~/Downloads/neemba.pem`(`~/.ssh/neemba-key.pem` 거부). **SG의 22포트는 단일 IP 핀 — IP 바뀌면 콘솔에서 갱신(테라폼 미흡수 상태)**.
- **디스코드 웹훅 함정 2건(실측)**: ① URL 복사 시 ID/토큰 사이 슬래시 유실 사례 — 형식 `/webhooks/<숫자ID>/<토큰>` 7세그먼트 확인 ② Cloudflare가 `Python-urllib` UA의 POST만 403(GET·curl은 통과) — monitor는 UA 명시로 해결됨, 새 클라이언트 작성 시 주의.
- **monitor 운영**: 상태 초기화는 반드시 `docker stop monitor` 후 볼륨에서 state.json 삭제(살아있는 프로세스가 마지막 틱에 옛 상태 재저장하는 레이스). 심장박동이 클라 끊김(재접속 대기) 중에도 우는 것은 의도(청중이 자막 못 보는 상태). dev compose(gitignore)에는 가속 파라미터(INTERVAL=10·GAP=30)가 들어 있음.
- **검증 하네스**: `services/node/scripts/ws_probe_dev.cjs`·`services/python/scripts_dev_publish.py`가 로컬 미커밋 — 재사용 시 커밋 결정.
- **재시도 버퍼 TTL은 JetStream dedup 창(2분) 미만 유지 필수** — 2분 이상 올리면 재발행 중복 방어 전제가 깨짐(retryingPublisher.ts 주석).
- **NATS 비밀번호는 영문자로 시작**. **nginx-rtmp `worker_processes 1` 유지 필수**·`depends_on: node` 필수. 외부 클라이언트 생성부는 lazy로(PR #28). prod compose `${}` 치환 → 배포 명령 `--env-file` 필수. dev 재기동: `docker compose -f docker-compose.dev.yml --env-file .env.dev up -d --force-recreate`.
- **모니터 페이지 Basic Auth**: `monitor` / `/var/lib/neemba/secrets/monitor.htpasswd`. **운영 DB 접속**: SSH 터널 + postgres 컨테이너 IP(재배포 시 변동).
- **AWS 비용**: hymn은 별도 실서비스(내리면 안 됨). Savings Plan 1년 약정(~2027-07-19).
- 장애 전말: `docs/incidents/`. 재현 절차: `docs/repro-2026-07-19-fixes.md`, `scripts/repro_publish_retry.ts` 헤더.

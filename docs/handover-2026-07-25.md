# 인수인계 문서 (2026-07-25)

> 기존 handover-2026-07-22.md를 대체하는 단일 문서 (구버전은 git 히스토리에 있음).
> 완료 항목 세부는 git log(PR #45~#53, Neemba_Front #1)가 진실.

---

## 1. 작업 규칙

- **브랜치**: `main - develop - feature`. 모든 PR은 develop으로, develop→main은 release PR로만(머지 = 자동 배포). feature 브랜치는 머지 후 삭제. **mvp(Neemba_Front)도 동일 구조**(2026-07-20 세팅, 기본 브랜치 develop).
- **TDD**: red → 수정 → green + 전체 스위트 + `tsc --noEmit`/`py_compile` → 한국어 conventional commit → PR → 머지.
- **테스트**: Node `cd services/node && npm test && npx tsc --noEmit` (**75 passed**). Python `cd services/python && uv run --extra dev pytest tests/ -q` (**104 passed**). mvp `flutter analyze` (0 issues — **로컬·CI 모두 stable 3.44.8 고정**, 채널 바꾸면 CI와 어긋남).
- **gh 계정**: `pro047`. auto-merge 꺼짐 — 체크 통과 대기 후 수동 머지. 로컬 origin은 https(gh 인증 push).
- **branch protection**: develop/main에 `node-test`·`python-test` 필수 체크.
- **AWS 변경은 `infra/terraform/`에서 코드→plan→apply로만.** state=`s3://hymn-tfstate/neemba/terraform.tfstate`. (EC2 인스턴스는 의도적 미흡수 — backup.tf 헤더의 replacement 위험 참조)

## 2. 현재 가동 중인 운영 체계 (전부 배포·검증 완료)

| 체계 | 구현 | 비고 |
|---|---|---|
| CI 테스트 게이트 | deploy.yml `test` 잡 + ci.yml | 실패 시 배포 미시작 + 머지 차단 |
| **GHA AWS 자격증명 = OIDC** | **neemba-gha-deploy(SG 열닫만, trust=main)·neemba-gha-watch(cloudwatch 조회만, trust=main+develop) — 정적 키 GitHub 저장 소멸** | 2026-07-25 배포·3경로 검증(§3). 구 키는 Inactive(§4-5) |
| 인증서 D-14 감시 | cert-watch.yml — 매일 06:00 KST | 2026-10-10 만료 |
| /health 외부 감시 | health-watch.yml — 5분 cron + 디스코드 | 서버 밖 시점 계층 — 유지 |
| CPU 크레딧 누수 감시 | cpu-credit-watch.yml — 매일 06:00 KST | 임계 60크레딧/24h. OIDC 전환됨 |
| Postgres 백업 | pg-backup 사이드카(S3, 90일) | 복원 리허설 완료 |
| RTMP 안정화 | ffmpeg 백오프·on_publish 즉시 재시작·워커 1 고정 | 인증은 **아직 꺼짐**(§4-1) |
| ws 세션 복구 | 끊김 즉시 인지 + pending 큐 + 재접속 방류 | 7/22 예배에서 실전 PASS |
| publish 재시도 버퍼 | NATS 순단 시 span 보존→순서대로 재발행 (TTL 60s·캡 500) + NATS 무한 재연결 + 스트림 max_age 600s 정합 | 재현: `services/node/scripts/repro_publish_retry.ts` |
| 파이프라인 알림 사이드카 | monitor 컨테이너 — /metrics 폴링(60s), 심장박동·에러 시그니처 9종·상태전이 dedup·디스코드 | 웹훅은 ENV_PROD의 `DISCORD_WEBHOOK_URL` |
| AWS 테라폼 | 백업 자원 + **SG + GHA OIDC 롤** 흡수, drift 0 | EC2 인스턴스만 미흡수(의도적 보류) |

## 3. 이번 기간(2026-07-25) 완료

| 작업 | PR | 요지 |
|---|---|---|
| dev 검증 스크립트 커밋 | #51 | ws_probe_dev.cjs(가짜 청중)·scripts_dev_publish.py(가짜 발행자) — 7/22 잔여물 해소, 헤더에 컨테이너 실행 usage. §4-4 다중 청중 시뮬레이션에 재사용 예정 |
| §4-5 1단계: GHA OIDC 전환 | #52 | 목적별 롤 분리(쓰기=deploy는 main만, 읽기=watch는 develop 포함해 배포 전 dispatch 검증 가능). SG 테라폼 흡수 — 개별 규칙 리소스 방식(인라인 ingress는 GHA 임시 규칙·운영자 22핀을 지워버림). imports.tf 소진·삭제. apply: 5 imported/4 added/**0 changed**/0 destroyed |
| release 배포 | #53 | OIDC 첫 실배포 성공: deploy 롤 assume(AROA 확인)·SG allow→revoke 정상·prod /health ok. 검증 3경로(watch@develop·deploy@main·watch@main) 전부 PASS |
| 구 GHA 키 비활성화 | — | `AKIA...PPXH` Inactive (삭제 아님 — 문제 시 Active 복구). 검증 완료 **후** 비활성화한 순서가 핵심(§5) |

## 4. 다음 작업 (우선순위 순)

### 4-1. ~~RTMP 인증 활성화~~ → **완료(2026-07-26)**
키 생성 → 로컬 `.env.prod` `RTMP_PUBLISH_KEY` 추가 → `gh secret set ENV_PROD` → main 배포 rerun(run 30144991353, deploy.yml은 push 트리거뿐이라 rerun으로 시크릿만 반영 — develop 미배포분 안 끌려감) → 검증 3종 통과: 키 없음 거부·틀린 키 거부(ffmpeg 실측)·올바른 키 수용(OBS 실송출, ffprobe로 스트림 흐름 확인).
- **교회 OBS는 obs-multi-rtmp 플러그인으로 이중 송출** (방송 탭 = YouTube 계정 연결 — 절대 건드리지 말 것). neemba 키는 multi-rtmp 도크의 대상 항목에 있음: `translation?key=<비밀값>`.
- 키 위치 3곳(로테이션 시 전부 갱신): 로컬 `.env.prod` / GitHub 시크릿 `ENV_PROD` / 교회 OBS multi-rtmp.
- 잔여 확인: 내일부터 "RTMP 인증 꺼짐" 일일 리마인더가 안 오면 완전 종료.
- **부분 회귀(2026-07-26 당일)**: 유튜브(비공개)+multi-rtmp 동시 송출 상태에서 키 publish 수용, API 세션 시작, ffmpeg pull·STT·번역기 스핀업까지 확인. **음성→자막 E2E는 마이크 부재로 미실시 → 수요일(7/29) 저녁 앰프·마이크 연결 상태에서 회귀테스트 예정** (절차: 비공개 유튜브 방송 + multi-rtmp 시작 → 앱 또는 API 세션 시작 → 마이크 한국어 발화 → 모니터 페이지에서 영어 자막 확인 → 세션·방송 종료).
- 주의: 7/26 12:5x경 테스트 세션이 무음으로 수 분 열려 있었음 — 그 시간대 심장박동/STT 일시정지 디스코드 알림은 테스트 오탐이니 무시.

### 4-2. 다음 예배(7/26): 재시도 버퍼·모니터링 첫 실전 관찰
알림 없음 = 정상. 순단 발생 시 심장박동·복구 알림 도착과 `publish buffer: retrying` 후 자막 유실 없음 확인. OIDC 체제 첫 예배이기도 함(배포는 7/25 완료 — 예배 전 추가 작업 없음).
→ **관찰 결과(2026-07-26 실측) — 완료**: /ws 순단 3회(01:57 UTC 30초 / 02:08 UTC 63초 / 02:19 UTC 4.7초, ~11분 간격) — 재연결 버퍼가 매번 동작(`flushed pending` 21·2건), **자막 유실 0건(첫 실전 성공)**. 디스코드 알림 없음도 규칙상 정상 확인(최대 공백 63초 < 심장박동 임계 180초, 유실·send 실패 카운터 0). publish 경로 순단은 발생 자체 없음. 서버 쪽 원인 배제 — 세 번 모두 교회 현장 기기(Dart UA, 현장 IP)가 끊음. 원인 계측은 4-7.

### 4-3. mvp 후속
mic 탭 실동작 검증(실기기 필요 — 현재 기기 없음)만 잔존. ~~서버 TTS 백엔드 확인~~ → **`/api/mic/tts`는 node에 구현·테스트 존재 확인됨(2026-07-25)**. ~~mvp CI+branch protection~~ → **완료(2026-07-25, Neemba_Front PR #2)**: ci.yml(`flutter-analyze` job, stable 3.44.8 고정) + develop/main protection(Back과 동일 구조). 겸사겸사 Flutter beta 3.32.0-0.5.pre→**stable 3.44.8 전환**(pubspec.lock 재해석) + 린트 전면 정리 — analysis_options의 `plugins`가 linter.rules에 잘못 들어가 flutter_lints 전체가 무력화돼 있던 것 수정(숨은 린트 10건 해소).

### 4-4. /ws 인증 + 다중 청중 허브 재설계
현 WebSocketHub는 단일 슬롯(동시 1클라) — per-connection 구조로 재설계. Flutter 수정 동반이라 한 세션에서. **주의: hub_active_session 등 메트릭·심장박동 게이트도 같이 갱신 필요.** dev 시뮬레이션에 ws_probe_dev.cjs 다중 기동 활용.

### 4-5. IAM §4-5 2단계 — 로컬 ljs 강등 + 잔여 정리
- **관찰 기간(예배+정기 감시 며칠) 후**: 키 `AKIA...PPXH` 삭제 + GitHub 시크릿 `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` 삭제(`AWS_REGION`은 계속 사용 — 유지)
- 로컬 강등(별도 세션, root 로그인 가능 확인됨): neemba-admin 롤(MFA 조건) 생성 → **assume 검증 후에야** ljs에서 AdministratorAccess 분리 → 잉여 정책(s3-db·UpdateGhaSshIps) 제거. **로컬 키 `...EHHR`은 hymn 테라폼도 사용 — hymn 작업에도 상승 프로파일 필요**

### 4-6. 백로그
디스크 사용률 감시(호스트 / ro 마운트 트레이드오프 결정 필요) / 드롭 카운터 외 메트릭의 Grafana 대시보드(알림 체계 안착 후) / GHA 감시 3종 dedup / DeepL 클라이언트 재사용·비동기화 / VAD rotation / 찬송 모드 / Makefile·ESLint / GHA Node20 버전업(actions 다수가 Node24 강제 경고 중) / terraform S3 native lock / postgres 루프백 공개 / main.py pong send 게이트 밖(구 REV-5 H3).

### 4-7. /ws 순단 계측 — close code 로깅 + ws_blips DB 기록 (별도 소PR, monitor-v2와 무관)
2026-07-26 예배 중 /ws 순단 3회의 원인 판별·이력화용. 실제 우선순위는 4-4 재설계보다 앞(4-4 착수 전에 데이터 확보). **범위 확정(2026-07-26 사용자 결정)**: 기록+조회 API까지 이 소PR, 화면 노출은 monitor-v2 WU5.
- **[python]** `/ws` 핸들러의 `WebSocketDisconnect`에서 **close code 로깅** 추가: 1001(클라 정상 종료 = 기기 절전/앱 백그라운드) vs 1006(비정상 단절 = 네트워크) 구분이 핵심.
- **[python+DB]** 순단 이벤트 **DB 테이블 `ws_blips` 기록**(사용자 확정 — 메모리 아닌 DB): session_id·끊긴 시각·재연결 시각·지속 초·flush 건수·유실 건수·close code. hub의 disconnect/reconnect 지점에서 기록, 기존 `/api/monitor/*` 패턴으로 조회 API 1개.
- **[선택, Flutter]** 앱 쪽 재연결 로그(끊김 직전 백그라운드 전환·네트워크 상태) — 실기기 검증(4-3)과 묶어도 됨.
- **현장 조치(코드 무관)**: 자막 기기 화면 꺼짐/절전 해제, Wi-Fi↔LTE 바꿔서 다음 예배 순단 빈도 비교.
- 가설 우선순위: ① 기기 Wi-Fi 불안정(AP 로밍) ② 기기 절전/백그라운드 ③ 공유기 NAT 정리. 서버 원인은 실측으로 배제됨(4-2 관찰 결과).

## 5. 미해결·주의사항 (이월+신규)

- **서버 실 IP = 13.125.26.93** (EIP, i-067ebdc3337b14ff9, t3.medium). SSH 키 `~/Downloads/neemba.pem`(`~/.ssh/neemba-key.pem` 거부). **SG는 테라폼 관리되지만 22포트 운영자 핀 규칙은 의도적 비관리 — IP 바뀌면 지금처럼 콘솔에서 갱신**(sg.tf 헤더 참조).
- **OIDC 함정 3건(실측)**: ① job-level `permissions:` 블록은 기본 토큰 권한을 **대체** — checkout 있는 잡엔 `contents: read` 필수 ② trust `sub`를 `repo:pro047/*`처럼 넓히면 아무 저장소나 assume 가능(hymn 롤이 그 상태 — neemba는 저장소·브랜치 명시) ③ 구 키 비활성화는 main 워크플로가 완전히 OIDC로 교체된 **뒤에만** (어기면 정기 감시가 인증 실패 → 오탐 디스코드 알림).
- **SG import 교훈**: import는 plan에서 **0 changed** 확인 후 apply — 첫 plan이 Name 태그 삭제를 드러내 수정했음.
- **디스코드 웹훅 함정 2건(실측)**: ① URL 복사 시 ID/토큰 사이 슬래시 유실 사례 — 형식 `/webhooks/<숫자ID>/<토큰>` 확인 ② Cloudflare가 `Python-urllib` UA의 POST만 403 — monitor는 UA 명시로 해결됨, 새 클라이언트 작성 시 주의.
- **monitor 운영**: 상태 초기화는 반드시 `docker stop monitor` 후 볼륨에서 state.json 삭제(레이스). 심장박동이 클라 끊김 중에도 우는 것은 의도. dev compose(gitignore)에는 가속 파라미터(INTERVAL=10·GAP=30).
- **검증 스크립트**: `services/node/scripts/ws_probe_dev.cjs`·`services/python/scripts_dev_publish.py` — 커밋됨(PR #51), 헤더에 usage.
- **mvp CI 함정(실측)**: `.env*`가 gitignore이면서 pubspec assets로 선언돼 있어 CI에서 `touch .env .env.dev .env.prod` 없이는 analyze가 asset_does_not_exist로 실패(ci.yml에 처리됨). `flutter test`는 유일 테스트가 `.env` 내용 검사라 CI 미포함 — 의미 있는 테스트 생기면 job에 추가.
- **재시도 버퍼 TTL은 JetStream dedup 창(2분) 미만 유지 필수** (retryingPublisher.ts 주석).
- **NATS 비밀번호는 영문자로 시작**. **nginx-rtmp `worker_processes 1` 유지 필수**·`depends_on: node` 필수. 외부 클라이언트 생성부는 lazy로(PR #28). prod compose `${}` 치환 → 배포 명령 `--env-file` 필수. dev 재기동: `docker compose -f docker-compose.dev.yml --env-file .env.dev up -d --force-recreate`.
- **모니터 페이지 Basic Auth**: `monitor` / `/var/lib/neemba/secrets/monitor.htpasswd`. **운영 DB 접속**: SSH 터널 + postgres 컨테이너 IP(재배포 시 변동).
- **AWS 비용**: hymn은 별도 실서비스(내리면 안 됨). Savings Plan 1년 약정(~2027-07-19).
- 장애 전말: `docs/incidents/`. 재현 절차: `docs/repro-2026-07-19-fixes.md`, `scripts/repro_publish_retry.ts` 헤더.

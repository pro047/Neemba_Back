# 인수인계 문서 (2026-08-02) — 현황·우선순위 편

작성: 2026-08-02 (주일예배 prod 모니터링 세션) · 발주자: jinseong

**이 문서의 범위는 "지금 어디까지 왔고 다음에 뭘 하느냐" 하나다.**
개별 작업의 설계·절차는 각 계획 문서에 있고 여기서 복제하지 않는다.

- 세부 작업 규칙·운영 체계·함정 목록 → **`docs/handover-2026-07-25.md` 가 계속 유효**
  (§1 작업 규칙, §2 운영 체계, §4-3~4-6 백로그, §5 주의사항). 이 문서는 그것을
  대체하지 않고 **현황 레이어만 갱신**한다.
- 세션 수명주기·상태 정합성 → `docs/session-lifecycle-plan.md`
- 모니터 페이지 v2(WU1~5) → `docs/monitor-page-v2-plan.md`
- 모니터 스크롤 UX·도메인 분리 → `docs/monitor-page-v3-plan.md` (2026-08-02 신설)

---

## 1. 한눈에 보는 현황 — prod 는 develop 보다 한참 뒤에 있다

**prod = release #57 (2026-07-26 배포). 그 뒤 develop 에 6개가 쌓였고, PR 2개가 더 대기 중이다.**

2026-08-02 prod 실물에서 마커를 grep 해 확인했다 (`#60` 게이트 0건,
WU-A `publish_done` 0건, `app.js` 는 7/26 15:53 빌드본).

| 항목 | prod | develop | 열린 PR |
|------|------|---------|---------|
| WU1 전역 이벤트 채널 | ✅ | ✅ | |
| ws_blips 순단 계측 (§4-7) | ✅ | ✅ | |
| WU2 캡처 경로 개편 | ❌ | ✅ #58 | |
| WU3 프런트 TS 마이그레이션 | ❌ | ✅ | |
| WU4 프런트 실시간 기능 | ❌ | ✅ #59 | |
| WU-B monitor 경보 게이트 | ❌ | ✅ #60 | |
| WU-A publisher 종료 시 세션 자동 종료 | ❌ | ✅ #62 | |
| WU5 시스템 상태 개요 | ❌ | ❌ | **#61** |
| WU-D 게이지 종료·부팅 경로 | ❌ | ❌ | **#63** |

**이 격차가 오늘 예배에서 관측된 문제 대부분의 원인이다.** 오탐 경보(#60 미배포),
게이지 고착(#63 미배포), 모니터 화면 기능 부재(WU2~4 미배포)가 전부 여기서 나온다.
**"prod 에서 안 된다" 를 버그로 판정하기 전에 이 표를 먼저 볼 것.**

## 2. 우선순위 큐

| 순서 | 항목 | 상태 | 비고 |
|------|------|------|------|
| 1 | PR #63 (WU-D) 머지 | 구현·테스트 완료 | |
| 2 | PR #61 (WU5) 머지 | 구현·검증 완료 (pytest 143) | |
| 3 | **WU1~WU5 통합 리뷰** | **미착수** | v2 §4-5 요구. **구현과 다른 세션에서** (자기검증 편향 차단) |
| 4 | release PR (develop → main) | 미착수 | |
| 5 | 배포 후 확인 3종 | — | WU-C 고아 세션 자동 정리 / monitor 첫 틱 / `ended_at IS NULL = 0` |
| 6 | WU-F 상태 수명주기 정합성 | 미착수 | `session-lifecycle-plan.md` §11 (2026-08-02 신설) |
| 7 | V3-B 모니터 도메인 분리 | 미착수 | `monitor-page-v3-plan.md` |
| 8 | V3-A 자막 스크롤 UX | 미착수 | **5번 배포 후** 재현 확인이 선행 |

**3번이 현재 유일한 병목이다.** 코드는 다 있는데 리뷰가 없어서 릴리스가 안 나가고,
릴리스가 안 나가서 prod 문제가 계속 재발한다.

**WU-F(6번)를 릴리스에 넣을지는 사용자 결정 사항** — 넣으면 배포가 늦어지고,
빼면 `ws_blips` 오염이 다음 예배에도 반복된다.

## 3. 아직 사용자 결정이 안 난 것

`session-lifecycle-plan.md` §5 의 4건이 그대로 미응답이다.

1. 자동 종료 시 디스코드 정보성 알림을 보낼까? (매 방송마다 오면 소음)
2. `close_stale_sessions` 를 startup 1회 → 1시간 주기로 바꿀까?
3. WU-A 를 #60·#61 과 같은 release 에 묶을까, 따로 갈까?
4. 리뷰 잔여 8건 중 **순단 탭 페이지네이션 2건**을 이번 릴리스에 넣을까?

여기에 2026-08-02 자로 하나 추가:

5. **WU-F 를 이번 릴리스에 넣을까, 다음으로 미룰까?**

## 4. 2026-08-02 주일예배 관측 기록

**서버측 장애 0건.** 발생한 문제는 전부 ① 기기측 네트워크 ② 미배포 코드로 인한
오탐·기록 결함이다.

| 항목 | 결과 |
|------|------|
| 번역 총량 | 약 550건 (10:51~11:27 KST, 37분) |
| 순단 | **3회 — 전부 close_code 1006**(기기측 비정상 단절) |
| 자막 유실 | 1회차 0건 / 2회차 **span 5개** (세션 5회 교체 중) |
| 번역 품질 | 정상. 찬양 구간 단어 파편화 외 문장 잘림·중복·빈 문자열 없음 |
| ffmpeg·NATS·컨테이너·5xx | 전부 정상 (`ffmpeg_stale` 증분 0) |

### 4-1. 순단 3회 — handover §4-7 가설 ① 쪽 근거가 쌓였다

```
02:02:17 → 02:02:18 UTC   1초,  flushed 4,  유실 0   (정상 복구)
02:13:20 → (재연결 없이 02:13:27 세션 종료)          → 세션 5회 교체로 이어짐
02:27:36 → (재연결 없음, keepalive 300s 타임아웃)     → 예배 종료
```

**7/26 예배 순단 3회도 전부 1006 이었다.** 두 예배 연속으로 1001(기기 정상 종료)이
한 건도 없다는 것은 절전·백그라운드 전환(가설 ②)보다 **네트워크 불안정(가설 ①)**
쪽을 가리킨다. 다만 §4-7 이 제안한 현장 조치(Wi-Fi↔LTE 전환 비교)는 아직 미수행이라
확정은 못 한다.

### 4-2. 세션 5회 교체 — **원인 미확인**

```
02:13:27  세션 2d3aa2ab 종료 (끊긴 지 7초 만에 = 명시적 stop 호출)
02:13:27  → ca1e261b  (21초 후 종료)
02:13:48  → d4039f1b  (48초 후 종료)
02:14:37  → fd6ae34b  (96초 후 종료)
02:16:13  → e01b46f0  ( 8초 후 종료)
02:16:21  → 0d79c3ef  ← 안정화
```

각 종료마다 `publish buffer: span dropped after stop` 이 찍혀 **총 5개 span 이
유실**됐다. keepalive 는 300초까지 기다리게 돼 있는데 7초 만에 `stopped` 가 찍혔으니
서버가 죽인 것이 아니라 **명시적 stop 호출**이다.

**사용자 확인 결과: 본인이 한 것이 아니다.** 현장 다른 조작인지 자막 앱의 재기동
루프인지 미확정. 다음 예배에서 같은 패턴이 재발하는지 관찰할 것.

### 4-3. 발견된 결함 3건 → WU-F 로 묶음

전부 **"성공 경로에서 세운 상태를 종료·교체 경로에서 안 내린다"** 는 같은 패턴이다.
상세는 `session-lifecycle-plan.md` §11.

1. `neemba_stt_paused` 게이지 고착 → **WU-D(PR #63)로 이미 수정됨.** prod 미배포라 관측된 것
2. `ws_blips` pending 행이 세션 종료 시 안 닫힘 → 다음 세션 재연결이 3.6일짜리
   가짜 행을 만들고, **그 사이 순단은 아예 기록되지 않는다**
3. `neemba_publish_buffer_dropped_total` 이 인스턴스 교체 시 0 으로 덮임 →
   자막 유실 5건이 메트릭상 0건으로 보고됨

## 5. 신규 주의사항 (2026-08-02)

- **`scripts/update-gha-ssh-ips.sh` 의 revoke 가 거짓 성공을 보고할 수 있다.**
  실측: 룰 설명이 `temp operator ssh 2026-08-02` 인데 스크립트에 `temp operator ssh`
  를 넘기면, AWS 가 에러 대신 `UnknownIpPermissions` 를 돌려주고 스크립트는 그
  필드를 안 봐서 "revokeed" 를 출력한다. **룰은 남는다.**
  → **회수 후 반드시 `describe-security-group-rules` 로 눈으로 확인할 것.**
  확실한 방법은 `--security-group-rule-ids <sgr-...>` 로 직접 지우는 것이다.
  스크립트 수정은 미착수 (`UnknownIpPermissions` 검사 추가 필요).
- **prod `/srv/neemba` 는 git 저장소가 아니다.** 배포 버전을 `git log` 로 못 본다.
  코드 마커를 grep 하거나 컨테이너 기동 시각으로 역산해야 한다.
- **번역문은 DB 에 없다.** public 스키마는 `alembic_version` 뿐이고 송출문은
  python 컨테이너 로그의 `hub: broadcast:` 줄에만 남는다. 로깅이
  `max-size 10m, max-file 3` 이라 긴 예배는 앞부분이 밀려 사라진다.
- **예배 모니터링 도구는 저장소에 없다(2026-08-02 사용자 결정).**
  `scripts/watch-service.sh`(5분 tick 프로브) + `.claude/skills/watch-service/SKILL.md`
  (런북) 둘 다 **작성자 로컬에만 존재한다.** 다른 기기·다른 사람은 못 쓴다.
  다시 필요하면 재작성하거나 작성자에게 요청할 것.
- monitor 사이드카가 `stt_paused` 로 오탐 경보를 반복하는 건은 #63 배포로 해소된다.
  그때까지 예배 중 알림은 무시할 것.

## 6. 미결정

- ~~`watch-service` 스킬·스크립트를 저장소에 넣을지~~ → **해소: 넣지 않는다**
  (2026-08-02 사용자 결정). `.gitignore` 예외 추가도 하지 않는다.
- `scripts/watch-service.sh` 의 SSH 키 경로가 `~/Downloads/neemba.pem` 하드코딩.
  `NEEMBA_SSH_KEY` 환경변수로 분리하기로 했으나 **의도적 보류**(사용자 결정).
- prod `ws_blips` 잔존 고아 행(현재 id 3) 정리 방법 — 마이그레이션 or 수동 UPDATE.

# 인수인계 문서 — 현황·우선순위 편 (최종 갱신 2026-08-22)

최초 작성: 2026-08-02 · 발주자: jinseong

**파일명은 최초 작성일이고, 이 문서가 현행 핸드오프다.** 다른 문서 4곳이 이 경로를
참조하고 있어 이름을 바꾸지 않는다.

**이 문서의 범위는 "지금 어디까지 왔고 다음에 뭘 하느냐" 하나다.**
개별 작업의 설계·절차는 각 계획 문서에 있고 여기서 복제하지 않는다.

**본문은 상시분(현황·우선순위·지뢰·미결정) + 최신 스트림 하나만 유지한다.**
지난 스트림의 세션 일지는 → `docs/handover-archive.md` (세션 시작 시 읽지 않는다).
2026-08-17 에 §4·§7~§10(2026-08-02·05·06·08 일지)을 그리로 내렸다. 절 번호는
원문 그대로 살아 있으므로 다른 문서의 "handover §8" 같은 참조는 아카이브에서 찾으면 된다.

- 세부 작업 규칙·운영 체계·함정 목록 → **`docs/handover-2026-07-25.md` 가 계속 유효**
  (§1 작업 규칙, §2 운영 체계, §4-3~4-6 백로그, §5 주의사항). 이 문서는 그것을
  대체하지 않고 **현황 레이어만 갱신**한다.
- 세션 수명주기·상태 정합성 → `docs/session-lifecycle-plan.md`
- 모니터 페이지 v2(WU1~5) → `docs/monitor-page-v2-plan.md`
- 모니터 스크롤 UX·도메인 분리 → `docs/monitor-page-v3-plan.md` (2026-08-02 신설)
- 마이크·RTMP 세션 슬롯 → `docs/mic-rtmp-session-slot-plan.md`
- 멀티 청취자 P1 → `docs/multi-listener-p1-plan.md`
- 게이지 sessionId 라벨화 → `docs/gauge-session-label-plan.md` (2026-08-17 배포 완료)
- 상태 API 세션별 pause 상세 → `docs/session-pause-detail-plan.md` + `-judge.md`
  (파이프라인 산출물. **설계는 사람 검토 미통과** — 감사 문서를 먼저 읽을 것)
- 성능 테스트 → `docs/perf-test-plan.md` (2026-08-22 승인. **P1 구현이 다음 진입점**)

---

## 1. 한눈에 보는 현황 (2026-08-20 갱신)

**prod = `4fc92ce` (release PR #104, 2026-08-20). develop 과 동일하다.**
배포 실행 `32378380800` 성공, 스모크 4종 통과(`/health` 200 · `/monitor` 301 ·
`monitor.neemba.app/api/monitor/status` 401 · SG `revoked and verified`).

배포와 검증을 섞지 말 것 — 아래 "미검증" 은 코드가 prod 에 올라간 것까지만 사실이다.

| 항목 | prod | 배포 경로 |
|------|------|-----------|
| WU1 전역 이벤트 채널 | ✅ | release #57 |
| ws_blips 순단 계측 (§4-7) | ✅ | release #57 |
| WU2 캡처 경로 개편 | ✅ | release #65 |
| WU3 프런트 TS 마이그레이션 | ✅ | release #65 |
| WU4 프런트 실시간 기능 | ✅ | release #65 |
| WU-B monitor 경보 게이트 | ✅ | release #65 (#60) |
| WU-A publisher 종료 시 세션 자동 종료 | ✅ | release #65 (#62) |
| WU-D 게이지 종료·부팅 경로 | ✅ | release #65 (#63) |
| WU5 시스템 상태 개요 | ✅ | release #66 (#61) |
| v2 통합 리뷰 수정 | ✅ | release #66 |
| 모니터 정적 자산 서빙 수정 | ✅ | release #68 (#67) |
| WU-F F-1 ws_blips 슬롯 반납 | ✅ 배포 · **실효 미검증** | release #72 (#70) |
| WU-F F-2 자막 유실 카운터 | ✅ 배포 · **실효 미검증** | release #72 (#71) |
| V3-A 자막 스크롤 UX | ✅ 배포 · **실동작 미검증** | release #74 (#73) |
| SG 회수 거짓 성공 수정 | ✅ | release #77 (#76) |
| 모니터 페이지 안정성 3건 | ✅ 배포 · **헤드리스 검증 완료** | release #80 (#79) |
| 멀티 청취자 P1 (세션당 소켓 팬아웃·멱등 join) | ✅ 배포 | #81 (8/9) |
| OBS 다중 publish 가드 (선착순 슬롯·스트림명 검사) | ✅ 배포 | #83 (8/9) |
| 순단 탭 키셋 페이지네이션 · 자동종료 알림 문구 | ✅ 배포 | #84·#85 (8/9) |
| 유튜브 수집 기각 · 죽은 yt-dlp/streamlink 제거 | ✅ | #86 (8/9) |
| 마이크·RTMP 세션 슬롯 분리 (전역 sessionStore 삭제) | ✅ 배포 | #87 (8/10) |
| prod nginx `/api/mic` WS 블록 — 마이크 폴백 개통 | ✅ 배포 | #89 (8/11) |
| 마이크 다중 세션 (단일 슬롯 제거·상한·미접속 정리) | ✅ 배포 | #91 (8/11) |
| 모니터 `monitor.neemba.app` 서브도메인 분리 | ✅ 배포 | #93 (8/12) |
| **게이지 sessionId 라벨화** | ✅ 배포 · **검증 완료** (2026-08-19 dev 합성 세션) | release #97 (#96, 8/17) |
| watch-service tick 에 한국어 원문(SOURCE) 수집 | ✅ develop · **배포 무관**(로컬 스크립트) | #99 (8/17) |
| **WS 만료 close code 계약** (4410 신설·업링크 접속 시점 검증·`ended` 통과) | ✅ 배포 · **검증** (prod 4404 실측 · 4410 은 dev 실측) | release #104 (#100, 8/20) |

**게이지 라벨화 검증 상태 (2026-08-17 prod 실측)**

모니터 **화면**으로는 신·구를 구분할 수 없다(세션 0개면 양쪽 다 0으로 보인다).
**하지만 원시 `/metrics` 는 구분된다** — 이 방법으로 배포를 확인했다:

```
docker exec node curl -fsS localhost:3000/metrics | grep -E '^neemba_(stt_paused|publish_buffer_size)'
# → 0줄  (신코드: 세션 0개면 샘플 라인이 없다. 구코드는 `neemba_stt_paused 0` 을 항상 냈다)
docker exec node curl -fsS localhost:3000/metrics | grep '^neemba_rtmp_auth_enabled'
# → neemba_rtmp_auth_enabled 1   (대조군: 무라벨 게이지는 샘플이 있다 = 스크레이프 실패가 아님)
```

**대조군을 반드시 함께 볼 것.** "0줄" 만 보면 스크레이프 실패와 구분되지 않는다.

확인된 것: ① 라벨드 게이지 형태로 배포됨 ② `# TYPE` 시딩 경로가 살아 있음 —
상태 API 가 `sttPaused:false`·`publishBufferSize:0` 을 반환(시딩이 없으면 `null`)
③ `rtmp_auth_enabled` 가 **시딩되지 않고 실제 값 1** 을 반환(리뷰 수정 #4 가 의도대로 동작).

~~**남은 미검증분**: 세션별 시리즈 독립성(은폐 시나리오)과 종료 시 시리즈 제거.~~
→ **해소 (2026-08-19, dev 스택 합성 세션 2개).** 상세·재현 절차는
`gauge-session-label-plan.md` §10. 결정적 관측은 한 스크레이프에 같은 게이지의
두 시리즈가 다른 값으로 공존한 것이다(`stt_paused{A}=1` · `{B}=0`) — 전역 게이지
하나로는 출력 자체가 불가능한 형태다.

**여기서 배운 것: "세션이 실제로 떠야 한다" 를 "예배가 있어야 한다" 로 읽지 말 것.**
게이지를 세우는 곳은 `StreamOrchestrator.ts:73-74`·`:143`·`:207` 뿐이고 마이크와
RTMP 가 둘 다 `createStreamOrchestrator` 를 지나므로 **게이지 경로는 세션 종류를
모른다.** `POST /api/mic/start` 가 무인증이라 합성 마이크 세션 2개면 충분했다.
이 오독 때문에 검증이 8/16 → 8/19 로 두 번 밀렸다.

**검증 방식은 항목의 성격이 정한다.** 모니터 안정성 3건은 예배 없이 헤드리스
Chrome 으로 재현·해소를 둘 다 봤다(9/9 + 음성 대조군 6건). 반면 WU-F·V3-A 는
**실제 세션이 떠야만** 판정된다 — 이 구분을 지키지 않으면 "테스트 통과" 를
"검증 완료" 로 착각하게 된다.

**그러나 "세션이 떠야 한다" ≠ "예배가 있어야 한다".** 게이지 라벨화가 이 오독으로
두 번 밀렸다(8/16·8/19). `POST /api/mic/start` 는 무인증이고 마이크·RTMP 가 같은
오케스트레이터를 지나므로, **합성 세션으로 판정되는 항목인지 먼저 따져볼 것.**
정말 예배가 필요한 것은 실제 발화·실기기·교회 네트워크가 걸린 항목뿐이다.

**"prod 에서 안 된다" 를 버그로 판정하기 전에**, 이제는 미배포가 아니라
**브라우저 캐시**를 먼저 의심할 것 (§5 참조).

## 2. 우선순위 큐 (2026-08-17 재정렬)

| 순서 | 항목 | 상태 | 비고 |
|------|------|------|------|
| ~~1~8~~ | ~~v2 WU 시리즈 · WU-F · V3-A · SG 회수 · 모니터 안정성 3건~~ | **완료** | release #65~#80. 경위는 `handover-archive.md` |
| ~~9~~ | ~~V3-B 모니터 도메인 분리~~ | **완료** | #93 (8/12). `monitor.neemba.app` |
| ~~1~~ | ~~게이지 라벨화 검증~~ | **완료 2026-08-19** | dev 합성 세션 2개로 3/3 PASS. `gauge-session-label-plan.md` §10. **배포 동결 해제됨** |
| **1** | 성경 고유명사 교정 (`transcriptNormalization`) | **방식 확정 · 데이터 수집 대기** | adaptation·모델 교체는 **기각**(실측 근거 §7). 원문은 node `published :` 로그에 이미 있다. **8/19 수요예배는 방송은 됐으나 번역이 실행되지 않아 수집 실패** → 8/23(일)이 다음 기회. 이건 실제 설교 발화가 있어야만 되는 유일한 항목이다 |
| **2** | **성능 테스트 P1 — 계기 3종 구현** | **계획 승인 · 구현 대기** | `perf-test-plan.md` §7. python 이벤트 루프 지연·DeepL 왕복·문장 큐 깊이. 변경 대상과 검증 명령이 계획에 확정돼 있어 **바로 착수 가능**. 예배와 무관 |
| 3 | 마이크 입력 품질 원인 확정 | **조사 완료 · 계측은 이미 앱에 있다** | 원인은 앱/플러그인(§7). **`mvp` `06a813c`(8/17)가 캡처 레이트 3중 비교를 이미 넣었다** — §7 의 "앱이 한 번도 읽지 않는다" 는 낡은 서술. 다만 `log.dart:14` 의 `logD` 가 `kReleaseMode` 에서 no-op 이라 **릴리스 빌드로는 한 줄도 안 찍힌다**. 기기에 debug/profile 빌드를 넣어야 데이터가 나온다 |
| 4 | 8/9 검증 3종(F-1·F-2·V3-A) 결과 | **상태 미확인** | 아래 근거. 완료로 적을 근거가 없다 |
| 5 | 세션 목록 인덱스 실측 | 미착수 | prod `count(*)` + `EXPLAIN`. SSH 붙는 김에 |
| 6 | prod `ws_blips` 고아 행 정리 | 미착수 | §6 참조. 지우기 전 현재 행 기록 |
| 7 | 상태 API 세션별 pause 상세 노출 | **설계·감사 완료 · 사람 게이트 대기** | `session-pause-detail-plan.md` + `-judge.md`. **근거 1건이 반박됐다**(§2-A-3 의 "두 게이지 시딩 시점이 다르다" → 실제로는 `StreamOrchestrator.ts:73-74` 인접 시딩). 계약 자체는 타당해 보이나 **판정은 사람 몫** |
| 8 | 모니터 폴러 사각지대 2건 | 미착수 | `handover-archive.md` §10 말미 |
| 9 | 게이지 PR 리뷰 후속 **정리** 3건 | 미착수 · 저우선 | `gauge-session-label-plan.md` §9 **말미**(§9 의 표는 이미 수정된 8건의 기록이지 할 일이 아니다 — 실제로 오독한 적이 있다). 잔여는 `watch-service.sh` 의 `eval`→`${!key}`, 컨테이너 로그 중복 스트리밍(node·python 을 각 3회 `docker logs`), 테스트 게이지 헬퍼 추출. **전부 청소이고 `watch-service.sh` 는 배포 대상도 아니다** |

~~**1번이 끝날 때까지 prod 배포를 동결한다.**~~ → **동결 해제 (2026-08-19).**
근거였던 검증이 dev 에서 끝났다. 동결의 취지는 "8/19 에 어긋났을 때 원인이 둘로
갈리는 것" 을 막는 것이었는데, 판정이 이미 났으므로 겹칠 대상이 없다.

**다음 송출(8/23)에서 볼 것** — 예배가 있어야만 되는 것들만 남았다:

1. **성경 고유명사 오인식 수집** (큐 1번) — `docker logs node | grep '^published :'`.
   **예배 후·다음 배포 전에 파일로 내려받을 것** (배포가 로그를 통째로 지운다)
2. **prod 첫 tick 에 라벨드 시리즈가 찍히는가** — 동작이 아니라 **배포·수집 경로**
   확인용이다. 동작은 dev 에서 끝났다
3. 8/9 미확인분(F-1 `ws_blips` 행 생성, F-2 유실 카운터) — **같은 SSH 세션에서**
   함께 본다. 붙는 비용이 같다 (큐 3번)

**4번이 "상태 미확인" 인 이유.** 8/9 주일예배는 실제로 있었고 그날 커밋도 여럿
나왔다(#81 멀티 청취자 P1, #83 OBS 다중 publish 가드, #84·#85 순단 탭·알림 문구).
**그런데 F-1·F-2·V3-A 의 검증 결과를 기록한 문서가 리포에 없다.** 커밋이 있다는
사실은 "그날 뭔가 관측했다" 는 정황일 뿐 검증 통과의 근거가 아니다. 확인되지 않은
것을 완료로 적지 않는다 — 다음 세션에서 사용자에게 확인하거나 prod `ws_blips` 를
직접 조회해 판정할 것.

**모니터 페이지를 처음 열 때는 하드 리로드(`Cmd+Shift+R`).** 콘솔에
`monitor: missing #...` 경고가 **하나도 없어야** 정상이다. 경고가 보이면 페이지
버그가 아니라 캐시가 안 비워진 것이다.

## 2-1. WS 재연결 계약 (2026-08-17 신설 — 클라이언트가 참조하는 값)

Flutter 재연결 예산의 근거다. **"서버가 끊김 후 300초 동안 pending 큐를 유지한다"
는 전제는 지금 코드에 없다** — 멀티 청취자 P1(#81)이 백로그(`_pending`)를 통째로
들어냈다(`websocket.py:17-20` 근거, `:616` 이 "300초 대기는 사라졌다"를 명시).
순단 구간 자막은 유실이고 유실량만 `ws_blips.lost_count` 로 남는다.

> **줄번호는 이 문서를 쓴 시점(2026-08-17) 기준이다.** 상수 이름으로 grep 하는
> 편이 안전하다 — 실제로 이 절의 초안이 자기 diff 때문에 어긋난 번호를 실었다.

### 세션 수명

| 항목 | 값 | 근거 (상수명) |
|---|---|---|
| 다운링크 끊김 후 유지 | **없음(무기한)** — 세션은 안 죽는다 | `websocket.py:364` `handle_client_disconnect` docstring |
| 업링크 끊김 후 유예 | **10초** → 세션 종료 | `DEFAULT_TEARDOWN_GRACE_MS` (`micWebSocket.ts:17`) |
| 업링크 반열림 탐지 | 15초 주기(최대 ~40초까지) | `DEFAULT_HEARTBEAT_INTERVAL_MS` (`micWebSocket.ts:21`) |
| start 후 첫 소켓 대기 | 15초 | `MIC_CONNECT_GRACE_MS` (`router/mic.ts:221`) |
| RTMP publisher_done 유예 | 120초 (env 조정) | `DEFAULT_PUBLISH_DONE_GRACE_SEC` (`router/rtmp.ts:14`) |
| 다운링크 keepalive | ping 30초 / pong 타임아웃 60초 (**소켓만** 버림) | `_PING_INTERVAL_SECONDS`·`_PONG_TIMEOUT_SECONDS` (`websocket.py:57-58`) |

**세션의 생사는 시간이 아니라 `_sessions` 등재 여부**이고, 빠지는 경로는 `detach`
하나뿐이다(`websocket.py:291`). 시간 기반 만료는 없다 — `close_stale_sessions` 는
startup 1회 도는 **DB 전용** 정리라 허브를 안 건드린다.

**따라서 마이크는 업링크가 세션 수명을 지배한다.** 업링크 백오프 상한이 10초를
넘으면 그 시점에 세션은 이미 죽어 있다.

### close code (2026-08-17 A그룹으로 정비)

| code | 의미 | 클라이언트 동작 | 근거 |
|---|---|---|---|
| `1008` | sessionId 쿼리 누락 | 중단 (버그) | `main.py:629` (다운링크) · `micWebSocket.ts` 접속 가드 |
| `4404` | 그 세션이 없다 | **재시도 중단** | `CLOSE_SESSION_NOT_FOUND` (`websocket.py:48`) · `CLOSE_MIC_SESSION_NOT_FOUND` (`micWebSocket.ts:27`) |
| `4410` | 세션이 방금 끝났다 | **재시도 중단** | `CLOSE_SESSION_ENDED` (`websocket.py:55`) |
| `1000` | 소켓 하나만 떨어짐 (keepalive 등) | **재시도 계속** | `_drop_conn` 이 기본값 유지 |

**1000 과 4410 의 구분이 핵심이다.** 정비 전에는 세션 종료도 1000 이라 클라이언트가
한 번 붙어서 4404 를 받아야만 만료를 알 수 있었다. 이제 종료 프레임 자체가 4410 이다.

**`_drop_conn` 에는 만료 코드를 쓰면 안 된다** — 소켓 하나가 keepalive 로 떨어졌을
뿐 세션은 살아 있고 그 청취자는 재접속을 **해야 하는** 쪽이다. 대조군 테스트가
`tests/test_ws_close_codes.py` 에 있다.

### 이 표의 실측 근거 (2026-08-20, release #104 배포 후)

**검증을 두 조각으로 갈라 각각 싼 곳에서 쟀다.** prod 에서만 알 수 있는 것은
"close code 가 nginx·TLS 를 통과하는가" 하나뿐이고, 코드 값 자체는 앱 계층 분기라
어디서 재도 같다.

| 확인한 것 | 어디서 | 결과 |
|---|---|---|
| close code 가 배관을 통과하는가 | **prod** — 미등록 sessionId 로 다운링크 접속 | `4404` |
| 종료가 4410 으로 나가는가 | **dev** — 세션 1개에 청취자 2명 붙이고 stop | `4410` ×2 |

- **prod 프로브는 세션을 만들지 않는다.** `wss://neemba.app/ws?sessionId=<아무거나>`
  에 붙으면 등재되지 않고 4404 로 닫힌다(B-2 화이트리스트) — DB 행도 STT 과금도
  디스코드 알림도 없다. **배포 직후 배관 확인에 이걸 쓸 것**
- dev 쪽은 `ws://python:8000/ws?sessionId=` 로 **nginx 를 건너뛰고** 앱 계층만 본다.
  배관은 위에서 이미 갈렸으므로 중복해서 잴 이유가 없다
- 청취자 **2명 모두** 4410 을 받았다 — 하나만 받고 나머지가 1000 으로 떨어지는
  회귀를 이 프로브가 잡는다

**교훈**: "prod 에서 확인해야 한다" 를 통째로 받아들이지 말고 **prod 고유 위험이
무엇인지 먼저 갈라낼 것.** 갈라내면 대개 한 조각은 공짜로 잴 수 있고, 나머지는
dev 에서 더 편하게 잰다. 게이지 라벨화 검증(§1)도 같은 방법으로 풀렸다.

업링크는 **접속 시점에** 검증한다(`micWebSocket.ts`) — 정비 전에는 첫 오디오
프레임이 와야 거절했다.

### `POST /api/mic/stop`

만료된 sessionId 로 불러도 **200** 이다(python stop 이 멱등). **200 을 "내가
끊었다" 로 읽지 말 것.** 대신 응답에 `ended` 가 실린다.

- `ended: true` — 이 호출이 세션을 끝냈다
- `ended: false` — **대개** 이미 끝나 있던 것이다 (오프라인 Stop 이 늦게 도달)

**`ended` 는 허브 상태가 아니라 DB 상태다** (`main.py:572-579` → `end_session`).
DB 풀이 없거나 `end_session` 이 예외를 내면 그 블록이 조용히 넘어가 `ended` 가
false 로 남는데, **그 뒤 `hub.detach` 는 정상 수행되어 세션은 실제로 끝난다.**
즉 RDS 페일오버·풀 소진 중에는 "내가 끊었는데 false" 가 나올 수 있고, **DB 없는
로컬 환경에서는 항상 false** 다.

→ 클라이언트는 `ended:false` 를 "이미 끝나 있었다" 로 **단정하지 말고** UI 를
어느 쪽으로도 무해하게(둘 다 '종료됨') 처리하는 편이 안전하다. 허브 기준의
정확한 답이 필요하면 `detach` 가 라이브였는지를 응답에 실어야 한다 — **후속
백로그**(`wasLive` 필드 추가, §6).

## 3. 사용자 결정 — 4건 전부 처리됨 (2026-08-09)

`session-lifecycle-plan.md` §5 는 아직 "미응답" 이라고 적혀 있다. **아래가 최신이다.**

| # | 질문 | 결론 |
|---|---|---|
| 1 | 자동 종료 시 디스코드 정보성 알림을 보낼까? | **이미 구현돼 있었다** — `d329f27`(WU-A), main 포함 확인. 질문이 열려 있는 동안 코드가 먼저 들어간 경우다. 문구만 P1 D4 기준으로 정정(PR #85, `258617b`) |
| 2 | `close_stale_sessions` 를 startup 1회 → 1시간 주기로? | **기각** (근거는 아래) |
| 3 | WU-A 를 #60·#61 과 같은 release 에 묶을까? | **실효** — `d329f27`·`d8ea29b`·`5faed0c` 전부 main 포함 확인 |
| 4 | 순단 탭 페이지네이션 2건을 언제? | **완료** — PR #84(`74333cf`), 키셋 전환 |

**2번 기각 근거 (다시 꺼내기 전에 반드시 읽을 것).** 지금 구현은 살아 있는 세션을
가려내지 않는다 — `_CLOSE_STALE_SESSIONS_SQL` 이 `WHERE ended_at IS NULL` **하나뿐이고
시간 조건이 없다**(`translation_repository.py:39-46`, 함수는 `:144`). startup 1회일
때는 "이전 프로세스가 죽으며 남긴 유령" 만 걸리므로 안전하지만, P1 D4 로 종료 경로가
`on_publish_done` 하나가 된 지금 **주기 실행하면 방송 중인 세션의 행을 종료로 찍는다.**
주기화하려면 "충분히 오래 조용한 세션만" 같은 조건이 선행돼야 하고 그건 별도 설계다.

5. ~~**WU-F 를 이번 릴리스에 넣을까, 다음으로 미룰까?**~~
   → **해소 (2026-08-02): 별도 release #72 로 나갔다.**

## 5. 신규 주의사항 (2026-08-02)

- ~~**`scripts/update-gha-ssh-ips.sh` 의 revoke 가 거짓 성공을 보고할 수 있다.**~~
  → **해소 (2026-08-06, release #77 / PR #76).** 스크립트가 회수 후
  `describe-security-groups` 로 실제 상태를 확인하고, 룰이 남아 있으면 exit 1 한다.
  **판정에서 description 은 보지 않는다** — 룰을 유효하게 만드는 것은 프로토콜·포트·
  CIDR 이고, 설명 불일치가 애초 이 버그의 계기였다. 검증 자체가 실패해도 실패로 친다.
  그 describe 를 위해 배포 롤 정책에 `ec2:DescribeSecurityGroups` 를 더했다
  (`infra/terraform/gha-oidc.tf`, apply 완료). 실 배포에서 확인된 성공 출력:
  `revoked and verified: tcp/22 from <IP>/32 is not open on sg-...`
  **`REVOKE FAILED` 가 뜨면 고장이 아니라 원래 조용히 남던 룰이 드러난 것이다** —
  회수 스텝은 `if: always()` 로 배포가 끝난 뒤 도는 정리 단계라 서비스 영향은 없다.
  이때는 `--security-group-rule-ids <sgr-...>` 로 직접 지운다.
  **여기서 배운 것**: AWS 는 "지울 걸 못 찾음" 을 에러가 아니라 응답 본문
  (`UnknownIpPermissions`)으로 알린다. exit code 만 보는 스크립트는 거짓 성공한다.
  **명령의 보고가 아니라 실제 상태로 판정할 것.**
- **prod `/srv/neemba` 는 git 저장소가 아니다.** 배포 버전을 `git log` 로 못 본다.
  코드 마커를 grep 하거나 컨테이너 기동 시각으로 역산해야 한다.
- **번역문은 DB 에 없다.** public 스키마는 `alembic_version` 뿐이고 송출문은
  python 컨테이너 로그의 `hub: broadcast:` 줄에만 남는다. 로깅이
  `max-size 10m, max-file 3` 이라 긴 예배는 앞부분이 밀려 사라진다.
- ~~**예배 모니터링 도구는 저장소에 없다(2026-08-02 사용자 결정).**~~
  → **절반 뒤집힘 (2026-08-17, PR #96).** `scripts/watch-service.sh` 는 **리포에 들어왔다**
  (게이지 라벨화가 이 스크립트의 awk 패턴을 깨서 같은 PR 로 묶어야 했다 — 소비자
  동시 갱신 원칙). 반면 `.claude/skills/watch-service/SKILL.md`(런북)는 **여전히
  gitignore 된 로컬 파일**이라 diff·PR 에 안 잡힌다. 스킬을 고쳤으면 **그 변경은
  아무 데도 기록되지 않으므로** 커밋 메시지나 계획 문서에 남길 것.
- ~~monitor 사이드카가 `stt_paused` 로 오탐 경보를 반복하는 건은 #63 배포로 해소된다.~~
  → **해소됨** (release #65, 2026-08-02 배포). 이제 이 알림이 뜨면 진짜로 볼 것.
- **배포해도 화면이 안 바뀌면 브라우저 캐시를 의심할 것 (2026-08-02 실제로 겪음).**
  `location /monitor` 에 캐시 지시가 없어 Chrome 이 휴리스틱 캐싱을 했고, WU5 를
  배포한 뒤에도 탭에는 옛 `app.js` 가 떠서 상태 칩 바·순단 이력 탭이 안 보였다.
  release #68 로 `Cache-Control: no-cache` 를 넣어 이후로는 자동 갱신되지만,
  **그 전에 캐시된 index.html 을 들고 있는 브라우저는 한 번 하드 리프레시가 필요하다.**
- **`neemba.app/monitor` 는 이제 `/monitor/` 로 301 된다** (release #68).
  슬래시 없이 들어가면 상대 경로 자산이 루트 catch-all(`200 "neemba"`)로 새서
  페이지가 통째로 먹통이었다 — Phase 6 이래 있던 버그. 404 가 아니라 200 이라
  콘솔 오류조차 안 나는 게 오래 안 잡힌 이유다.
- **prod 배포 확인은 SSH 없이도 된다.** `curl` 로 `/health`(200) ·
  `/monitor`(301) · `/api/monitor/status`(401, 404 아님 = 라우팅 정상) 만 봐도
  배포 반영 여부가 갈린다. 401 너머는 자격증명이 있어야 한다.
- **배포는 변경 범위와 무관하게 전 컨테이너를 재생성한다 (2026-08-05 확인).**
  `deploy.yml` 의 배포 스텝이 `docker compose up -d --build --force-recreate
  --remove-orphans` 라, 프런트 정적 자산만 바뀐 릴리스도 node·python·nginx 가
  전부 재시작된다. **"정적 파일만 바뀌었으니 안전하다" 는 추론은 성립하지 않는다** —
  판단은 diff 가 아니라 배포 파이프라인 기준으로. **송출 중에는 release 를 머지하지 말 것.**
- **자산 반영 확인은 "소스에 그 문자열이 있는가" 로 할 것 (2026-08-05 실패에서).**
  동작의 부수 효과로 버전을 역추론하면 안 된다. 실제로 겪은 오류:
  `closest('.table-wrap').scrollHeight === parentElement.scrollHeight` 로 신·구 버전을
  가르려 했는데, `scrollHeight` 는 레이아웃 값이고 app.js 는 `scrollTop` 만 대입하므로
  **어느 버전이든 항상 같은 값이 나온다.** 쓸 수 있는 형태:

  ```js
  fetch('app.js', {cache: 'reload'}).then(r => r.text()).then(t => console.table({
    '배너 DOM (index.html)':     !!document.getElementById('detail-new'),
    '스크롤 대상 교정 (app.js)':  t.includes('closest(".table-wrap")'),
  }));
  ```

  DOM 검사는 **지금 페이지가 실행 중인 버전**을, `fetch` 는 **서버가 주는 버전**을
  본다. 둘이 갈리면 캐시다. 서버만 단독 확정하려면 SSH 로
  `md5sum /var/www/html/monitor/app.js` 를 리포 파일과 대조한다(브라우저를 안 탄다).

### 2026-08-17 추가

- **python 테스트는 반드시 `.venv/bin/python -m pytest`.** 시스템 python 으로 돌리면
  `prometheus_client`·`nats` 미설치로 **수집 단계에서 12건 에러**가 난다. 이걸 코드
  결함으로 오독하기 쉽다 — 임포트 에러가 뜨면 먼저 인터프리터를 의심할 것.
- **커밋 게이트 토큰(`.git/REVIEW_ACK`)은 30분 만료이고, `touch && git commit` 을
  한 명령으로 묶으면 거부된다.** 훅이 명령 시작 시점에 토큰을 보므로 **touch 를
  별도 명령으로 먼저** 실행해야 한다. 오래된 세션에서 커밋하려다 두 번 막혔다.
- **라벨드 게이지는 세션이 0개면 시리즈가 0줄인 것이 정상이다.** 이때 모니터의
  "STT 동작" 초록은 **"일시정지 아님"** 이지 "STT 가 돌고 있다" 가 아니다
  (`# TYPE` 이 있으면 0.0 시딩 → 소비자는 0으로 읽는다). 세션 0개인데 초록이라고
  결함으로 판정하지 말 것. 세션별 상세 노출은 후속 백로그.
- **배포 직후 "마지막 자막 전송 없음" 은 장애가 아니다.** 이 값은 python 프로세스
  메모리의 게이지라 컨테이너 재생성으로 0 이 된다. 오히려 **배포가 반영됐다는
  방증**으로 읽으면 된다.
- **게이지 시리즈는 teardown 이 커버하는 스코프 안에서만 시딩할 것.** 조립 시점
  (`createStreamOrchestrator`)에 시딩했더니, `start()` 가 끝내 호출되지 않는 실패
  경로에서 **지울 수단이 없는 시리즈**가 남았다 — 이 PR 이 막으려던 cardinality
  누수를 시딩이 되살린 꼴이었다. 시딩 1곳 · 제거 1곳 대칭을 깨지 말 것.

### 로컬 dev 스택 — 현재 사실 (2026-08-09 실측, 2026-08-18 이관)

세 건 다 **디버깅 중에 시간을 통째로 날리는 종류**다. 하나는 리포의 다른 문서와
어긋나므로 여기가 정본이다.

- **node 를 고쳤으면 `docker restart node` 를 해야 그 코드가 돈다.** `tsx watch` 가
  macOS bind mount 의 변경을 못 잡는다 — 파일은 컨테이너 안에 들어가는데 프로세스가
  재실행되지 않는다(6시간 넘게 미반영을 실측). python 의 `uvicorn --reload` 는
  폴링이라 정상 반영된다.
  **`repro-2026-07-19-fixes.md:18` 은 "dev 는 `tsx watch` 라 저장 즉시 반영" 이라고
  적혀 있다** — 그 문서는 2026-07-19 시점의 재현 기록이라 소급 수정하지 않았다.
  현재 사실은 이 항목이다
- **앱의 `print()` 는 블록 버퍼링돼 `docker logs` 에 거의 안 뜬다.** 실시간으로
  보이는 건 uvicorn 액세스 로그뿐이다. 로그로 디버깅하려 하지 말고 게이지·DB·직접
  호출로 확인할 것
- **dev 와 prod 의 `DISCORD_WEBHOOK_URL` 이 같다.** dev 에서 테스트하면 **예배 알림
  채널로 알림이 섞여 들어간다**(2026-08-09 e2e 때 7건 발화). 테스트 전에
  `docker stop monitor` 하거나 `.env.dev` 의 웹훅을 비울 것
- **`nginx.conf` 를 고쳤으면 `docker restart nginx`** (2026-08-22 실측). 파일 단위
  bind mount 라 편집기가 inode 를 갈아치우면 **컨테이너가 잘린 뷰를 본다** — 호스트가
  165줄인데 컨테이너는 130줄을 보고 `nginx -s reload` 가 `unexpected end of file` 로
  실패했다. **호스트에서 돌린 `nginx -t` 는 통과하므로 문법 검사만 믿으면 원인을 못
  찾는다.** reload 실패라 서비스는 구 설정으로 살아 있었다(nginx 는 나쁜 설정을
  적용하지 않는다). `tsx watch` 함정과 같은 계열이다

## 6. 미결정

- ~~`watch-service` 스킬·스크립트를 저장소에 넣을지~~ → **해소: 넣지 않는다**
  (2026-08-02 사용자 결정). `.gitignore` 예외 추가도 하지 않는다.
  → **스크립트만 뒤집혔다 (2026-08-17, PR #96).** §5 첫 항목 참조. 스킬 런북은
  여전히 로컬 전용이다.
- `scripts/watch-service.sh` 의 SSH 키 경로가 `~/Downloads/neemba.pem` 하드코딩.
  `NEEMBA_SSH_KEY` 환경변수로 분리하기로 했으나 **의도적 보류**(사용자 결정).
- prod `ws_blips` 잔존 고아 행(현재 id 3) 정리 방법 — 마이그레이션 or 수동 UPDATE.
  ~~WU-F F-1 과 같이 처리하는 편이 낫다~~ → **F-1 은 release #72 로 배포됐고
  마이그레이션은 포함되지 않았다(2026-08-05 확인). 즉 근본 원인은 막혔고 고아 행은
  그대로다.** "행만 지우면 또 생긴다" 는 걱정이 사라졌으므로 지금이 정리할 시점이다.
  다만 **8/9 예배 전에 지우면 F-1 검증이 어려워진다** — 깨끗한 상태에서 시작해야
  "이번 예배에 생긴 행" 을 셀 수 있으니, 지우려면 예배 직전이 낫고 아니면 예배
  관측 후에 한다. 어느 쪽이든 지우기 전 현재 행을 기록해 둘 것.
- 세션 목록 상관 서브쿼리 인덱스(`app.translations (session_id, created_at DESC)`)
  — **prod 실측 후 판단**(2026-08-02 사용자 결정). 뜰 쿼리는 v2 §7 에 적어 뒀다.
  release #66 으로 30초 목록 폴링이 붙어 호출 빈도가 올라갔으므로 다음 예배 때
  `watch-service` 로 SSH 붙는 김에 같이 볼 것.
- **`/api/mic/stop` 에 `wasLive` 추가 여부** (2026-08-17 리뷰발). 지금 `ended` 는
  DB 기준이라 DB 장애 시 "내가 끊었는데 false" 가 나온다(§2-1). 허브 기준의
  정확한 답은 `hub.detach` 가 라이브였는지인데, 그걸 실으려면 `detach` 의 반환값
  변경 + python 응답 스키마 추가 + node 통과가 필요하다. **클라이언트가 이 구분을
  실제로 쓰는지 확인한 뒤 결정할 것** — 안 쓰면 스키마만 늘어난다.
- STALE 판정의 시계 스큐 — `startedAt`(서버) vs `Date.now()`(브라우저) 비교라
  부스 PC 시계가 3분 이상 앞서면 새 세션이 즉시 STALE 로 보인다. 서버 상대시각
  전환이 필요해 v2 범위 밖으로 뒀다. 실제로 겪은 적은 없다.


## 7. 최신 스트림 — 2026-08-16~22 (게이지 배포·검증 · WS 계약 · 성능 테스트 계획)

**한 줄**: 게이지 sessionId 라벨화를 커밋·배포하고(release #97) **dev 합성 세션으로
검증까지 끝냈다**. 예배 중 드러난 문제 2건(마이크 입력 품질·성경 고유명사 오역)은
조사가 끝났고 데이터 수집만 8/23 으로 남았다.

### 8/19~20 — 검증 완료 · 핸드오프 이중화 해소

**게이지 검증 (§1·§2 · `gauge-session-label-plan.md` §10)**: 8/19 수요예배는
**방송은 됐으나 번역이 실행되지 않아** 세션이 뜨지 않았다. 그 자리에서 "이 검증이
예배에 묶일 이유가 있나" 를 다시 따져 **dev 합성 세션 2개로 3/3 PASS.**
검증 도중 dev node 컨테이너가 `Up 10 days` 로 **8/17 커밋을 한 번도 안 돌리고
있었다**(`tsx watch` 함정) — 하마터면 멀쩡한 코드를 결함으로 판정할 뻔했다.

**핸드오프 이중화 해소 (PR #102)**: `.claude/handoff.md` 가 8/9 하루만 쓰이고
방치된 **추적조차 안 되는 로컬 사본**으로 남아 세션이 낡은 쪽을 먼저 읽는 사고가
났다. 619줄 전수 대조 후 리포에 없던 5건만 이관하고 삭제했다. 그중 dev 스택
함정 3건이 바로 다음 날 검증에서 두 건이나 적중했다(웹훅 공유 → `docker stop
monitor`, `tsx watch` → `docker restart node`).

**테스트 타입 사각지대 1건**: `tsconfig.json:49` 의 `include: ["src"]` 가 `test/`
를 빼고 vitest 는 타입을 안 봐서, `#100` 이후 fake 의 `stop` 이 실계약과 어긋나도
`tsc -p` 가 통과했다. 해당 1곳만 프로덕션 관용구(`router/mic.ts:455-457`)에 맞췄다.
**`include` 를 넓히는 것은 별건** — 다른 테스트에서 몇 건이 더 나올지 미지수다.

### 8/22 — 성능 테스트 계획 승인 · 관측 스택 정비

**계획은 `docs/perf-test-plan.md` 가 정본이다.** 여기에는 결론만 적는다.

**방향**: 이 시스템은 요청/응답 API 가 아니라 상시 스트림 파이프라인이라 RPS 로 재면
틀린 답이 나온다. **부하 축은 사용자 수가 아니라 발화 밀도(문장/분)** 이고, 근거는
DeepL 이 동기 블로킹인데 `_push_loop` 가 단일 태스크라는 것이다(`deepL.py`,
`kss_separator.py:207`). **문장 하나를 번역하는 동안 python 이벤트 루프 전체가 멈추고,
그 루프가 허브 브로드캐스트·keepalive·NATS 소비를 다 돌린다.**

여기서 나온 검증 가설: **발화 밀도가 오르면 자막 지연뿐 아니라 `ws_blips` 순단이
함께 발생한다.** 참이면 순단 이력은 네트워크 지표가 아니라 **부하 지표**이고 장애
조사의 해석이 바뀐다.

**관측은 Grafana 로 한다 — 모니터 페이지가 아니다.** 상태 API 파서가 라벨을 잘라
이름 키로 `max` 집계하므로(`node_metrics.py:74`·`:93`) **히스토그램이 통과하면 분포가
파괴된다.** 프런트에 차트 수단도 0이고(외부 CDN 0 방침) 30초 폴링이라 해상도도 안
맞는다. 역할이 다르다 — 모니터는 "지금 무슨 일이", Grafana 는 "지난 10분의 분포".

**관측 스택 정비 (이번에 실제로 고친 것)**: Prometheus·Grafana 가 이미 neemba dev
compose 에 있었고 node·python 을 15초마다 긁고 있었다. 다만 **nginx 잡이 DOWN**
이었다 — 익스포터가 안 떠 있었고, 띄워도 `stub_status` 가 **어느 nginx.conf 에도
없어** 404 였다. dev conf 에만 `location = /nginx_status`(내부망 한정)를 추가해
**타깃 3/3 `up`** 으로 만들었다. **prod conf 는 건드리지 않았다** — 연결·요청 수를
외부에 노출하는 엔드포인트라 결정 없이 늘리지 않는다.

**계측 재고에서 드러난 것**: node 는 `collectDefaultMetrics` 덕에
`nodejs_eventloop_lag_*` 8개 계열이 공짜로 나온다. **python 에는 없다** — 그리고
블로킹이 있는 곳은 정확히 python 이다. **node 쪽 공짜 지표를 보고 "이미 있네" 하면
정반대 결론에 간다.** 추가할 계기는 딱 3개(P1).

### 8/16 주일예배 관측 — 서버측 장애 0건

10:50~11:34 KST, 5분 tick 9회. 번역량 21~83건/5분, `ffmpeg_stale`·`hub_send_failed`·
`publish_buffer_dropped` 증분 **전부 0**, 컨테이너·NATS 정상, nginx 5xx 0.
`Stt error: 10 ABORTED ... no more client requests` 가 11:15 tick 에 1건 찍혔으나
번역은 계속 흐르고 있었고 재발이 없어 **일회성**(직전 마이크 테스트 세션의 잔여)으로 판정.

**이 예배는 라벨화 미배포 상태로 진행됐다** — prod 는 무라벨 전역 게이지였다.
계획서가 "8/16 이 첫 실전" 이라고 적어둔 검증은 그래서 8/19 로 이월됐다.

번역 품질: 문장 단위는 양호. **성경 고유명사가 반복적으로 깨진다** — "남유다" →
`Nam Joo-da's`, "아멘" → `Ira`/`Men`. 찬양·기도 구간 파편화는 알려진 정상 패턴.

### 게이지 라벨화 (PR #96 → release #97, prod `771aff2`)

커밋 전 `/code-review` 가 8건을 잡아 전부 수정하고 회귀 테스트 8건을 붙였다
(node 132→137, python 180→183). **가장 큰 건은 이 PR 이 막으려던 cardinality 누수를
시딩 위치 때문에 되살리는 결함**이었다 — 상세는 `gauge-session-label-plan.md` §9.
배포 실행 `31985118068` 성공, `/health` 200.

### 조사 착수 ①: 마이크 입력 품질 (§2 큐 3번)

**서버 쪽 결론은 났다 — 원인이 서버에 없다.** `micWebSocket.ts:130` 이 받은 바이트를
검증·변환 없이 그대로 PassThrough 에 쓰고, 오케스트레이터가 곧장 PCM 으로 취급해
STT 에 펌프한다. RTMP 는 반대로 `FfmpegTranscoder.ts:63-71` 에서 서버가
`-ac 1 -ar 16000 -acodec pcm_s16le` 로 직접 변환한다. STT recognizer 설정은 두 경로가
**동일**(LINEAR16/16k/mono, latest_long). 따라서 품질을 가르는 것은 앱이다.

**앱 저장소 = `/Users/ijinseong/Documents/translation/mvp` (Flutter, `flutter_audio_capture ^1.1.11`).**
조사 결과 **와이어 포맷 불일치는 없다** — mono 유지, 16-bit LE 변환 정확
(`lib/audio_capture_service.dart:6-17`), 컨테이너/코덱 미부착(`lib/node_ws_client.dart:168`),
16kHz 요청(`lib/audio_capture_service.dart:94`). "포맷이 깨져서" 가설은 기각.

유력 후보(강함 → 약함):

1. **iOS: 버퍼마다 리샘플러 재생성** — 플러그인의 `ios/Classes/AudioCapture.swift:60`
   에서 `AVAudioConverter` 를 **탭 콜백 안에서** 만든다. 48k→16k 필터 상태가 약 100ms
   마다 리셋돼 프레임 경계마다 불연속이 생긴다. 앨리어싱은 아니지만 초당 10회 미세
   글리치다. **RTMP 는 ffmpeg 가 한 번에 리샘플하므로 이 문제가 없다 — "마이크만
   나쁘다" 는 관찰을 가장 잘 설명한다**
2. **iOS `.measurement` 모드로 AGC 전무**(`AudioCapture.swift:17`) + 앱에도 게인·정규화
   없음. 화자와 거리가 있으면 저레벨 신호가 그대로 나간다. 앱에 이미
   `lowLevelFrameCount` 계측이 있어(`audio_capture_service.dart:115`) **이 비율로 확정 가능**
3. **TTS 재생이 오디오 세션을 덮어쓴다** — `audioplayers` 기본 iOS category 가
   `.playback` 이라 캡처용 `.playAndRecord/.measurement` 를 덮는다. AEC 도 없어
   스피커 → 마이크 되돌이가 원 발화와 섞인다
4. Android 플러그인이 `read()` 반환값을 무시하고 항상 버퍼 전체를 전송 → 부분 읽기 시
   직전 잔여 오디오가 붙는다
5. 재접속 시 3초 초과분을 무음 대체 없이 버려(`node_ws_client.dart:181-187`) 시간축이 붕괴

**가장 먼저 계측할 값**: 플러그인이 내려주는 `actualSampleRate` 를 **앱이 한 번도 읽지
않는다**(`lib/` 참조 0건). 하드웨어가 16k 를 못 주고 폴백해도 앱은 모른 채 16k 가정으로
보낸다. 실 캡처 레이트 로깅이 1순위다.

**주의**: 후보 1·3·4 는 전부 **플러그인/서드파티 쪽 코드**다. 앱에서 고치려면 세션
설정을 앱이 직접 잡거나 플러그인을 교체·포크해야 한다 — 범위가 작지 않다.

### 조사 착수 ②: 성경 고유명사 STT adaptation (§2 큐 2번)

현재 speech adaptation 은 **어디에도 적용돼 있지 않다**(`services/node/src` 전체에
`adaptation`/`phraseSet`/`boost` 0건). 위 오역은 발화가 뭉개져서가 아니라 인식기가
그 단어를 후보로 갖고 있지 않아서 생긴다.

**확정된 것 (공식 문서 근거)**

- 필드는 `RecognitionConfig.adaptation` (`SpeechAdaptation`). **인라인 phrase set·
  리소스 참조 둘 다 가능**하고 node 클라이언트 타입에도 전부 존재한다
- **recognizer 를 건드릴 필요가 없다.** `StreamingRecognitionConfig.config` 는
  `configMask` 가 없으면 **비-기본값 필드가 recognizer 기본값을 덮어쓴다** — 현재
  코드는 mask 를 안 보내므로 `adaptation` 한 필드만 추가하면 된다. **배포 영향이
  node 코드 한 곳으로 국한되고 롤백은 상수 비우기**다 → 리소스 방식보다 인라인 권장
- boost 는 0 초과 20 이하, 구절 단위 override 가능. 쿼터는 요청당 5,000구절·
  10만자·구절당 100자. 다어절 구절은 **통째로만** boost 가 걸린다
- 추가 과금 없음(가격 페이지에 adaptation SKU 자체가 없음). **지연 영향은 미문서화**

**실측 결론 (2026-08-17, Locations API 직접 조회) — 현재 모델로는 불가**

`https://speech.googleapis.com/v2/projects/{proj}/locations?pageSize=100&filter=language = ko-KR`
를 이 프로젝트 자격증명으로 조회한 결과다. **`pageSize` 를 안 주면 5개만 오고
`nextPageToken` 이 붙는다 — us-central1 이 그 뒤 페이지에 있어서 처음엔 안 보였다.
페이지네이션을 반드시 처리할 것.**

us-central1 의 ko-KR 모델 5개와 adaptation(`speech_adaptation_boost`) 지원:

| 모델 | adaptation |
|---|---|
| `long` | **✗** |
| `chirp_2` | **✓** |
| `chirp` · `telephony` · `usm` | ✗ |

**`latest_long` 은 응답에 아예 없다** — `long` 의 레거시 별칭이고, 그 `long` 이
ko-KR 에서 adaptation 미지원이다. `us`·`global`·`eu` 를 봐도 `long` 은 전부 ✗ 다
(✓ 인 건 `short`·`chirp_2`·`chirp_3`). 즉 **"리전을 바꾸면 된다" 가 아니라 어느
리전에서도 long 계열로는 안 된다.**

**실스트림 프로브 결과 (2026-08-17) — 결론: adaptation 은 접고 `transcriptNormalization` 을 쓴다**

TTS 로 합성한 한국어 8.3초("남유다 왕국의 히스기야 왕은 여호와께 기도했습니다.
북이스라엘과 앗수르 사이에서 예루살렘을 지켜 주소서. 아멘.")를 100ms 청크로
**실시간 속도로** 흘려 비교했다.

**재현 방법** (스크립트는 세션 scratchpad 에만 있었으므로 여기 요건을 남긴다):

- TTS `v1/text:synthesize` 로 `LINEAR16`/16000Hz 합성 → **44바이트 RIFF 헤더를 벗겨**
  raw PCM 으로 만든다(마이크 경로와 같은 형태)
- `recognizer: projects/{p}/locations/{loc}/recognizers/_` 인라인 설정 —
  **클라우드 리소스를 만들지 않는다**
- 스트림은 `client._streamingRecognize()` 로 열 것. **`client.streamingRecognize()`
  는 같은 요청에 `INVALID_ARGUMENT: Invalid resource field value` 를 낸다** — 이걸
  설정 오류로 오독해 30분 날렸다. 대조군(정규화 없는 케이스)도 같이 실패하는 걸 보고서야
  설정이 아니라 스트림 생성 방식 문제라는 게 드러났다
- 오디오는 **한 번에 밀지 말고** 100ms 간격으로 나눠 쓸 것 — 통째로 보내면 측정하려는
  interim 지연이 무너진다

| 케이스 | interim | 첫 interim | "남유다" | 변경 범위 |
|---|---|---|---|---|
| A. 현행 `latest_long` | 97건 | 3149ms | ✗ **나뮤다** | — |
| B. `latest_long` + adaptation | — | — | — | **INVALID_ARGUMENT** |
| C/D. `chirp_2` + adaptation | **1건** | **6136ms** | ✓ 남유다 | 모델 교체 |
| E/F. 도쿄 `chirp_2`·`chirp_3` | — | — | — | **PERMISSION_DENIED** |
| **G. `latest_long` + `transcriptNormalization`** | **97건** | **2459ms** | **✓ 남유다** | **필드 1개** |

읽는 법:

- **`chirp_2` 는 실시간 자막에 쓸 수 없다.** adaptation 은 실제로 걸리고 "남유다" 도
  맞히지만 **interim 이 1건뿐이고 6.1초 뒤에 온다** — 발화가 끝나야 자막이 뜬다는 뜻이다.
  고유명사를 얻고 실시간성을 통째로 잃는 거래다. (덤으로 "앗수르" 는 boost 를 걸었는데도
  "아수르" 로 **더 나빠졌다** — boost 가 만능이 아니라는 증거)
- **`latest_long` + adaptation 은 조용히 무시되지 않고 명시적으로 거부된다**
  (`Config contains unsupported fields`). 잘못 넣으면 바로 터지므로 안전하다
- **도쿄 이전안(B)은 불가능하다.** Locations API 목록에는 `chirp_2`·`chirp_3` 가
  나오는데 실제로 호출하면 `PERMISSION_DENIED: ... It is no longer generally available`
  다. **"목록에 뜬다 ≠ 쓸 수 있다" 를 실측으로 확인한 사례** — 앞으로 Locations 응답만
  보고 가능하다고 판단하지 말 것
- **G 가 답이다.** interim 97건·첫 interim 2459ms 로 **현행과 동등**(오히려 약간 빠름)
  하면서 "나뮤다→남유다", "북 이스라엘→북이스라엘" 이 교정됐다. 모델·리전·recognizer
  전부 그대로고 `config` 에 `transcriptNormalization` 한 필드만 추가한다

**interim 에도 교정이 걸린다** — 별도 프로브에서 미교정 interim 4건 / 교정된 interim
47건. 문서대로 stability > 0.8 부터 적용되므로 **발화 극초반 몇 건은 교정 전 텍스트가
나간다.** 우리 퍼블리셔는 interim 델타를 송출하므로 이 구간이 자막에 잠깐 스칠 수 있다.

**채택 시 트레이드오프 (정직하게)**: 정규화는 결정론적 문자열 치환이라 **틀린 형태를
일일이 등재해야 한다**("남유다" 를 넣는 게 아니라 "나뮤다" 를 넣는다). 새 오인식이
나올 때마다 항목이 늘어난다. adaptation 은 모델을 상류에서 편향시키는 근본 처방이지만,
그 대가가 실시간성 전부라면 이쪽이 낫다.

**데이터 출처 — 한국어 원문은 이미 node 로그에 있다** (`js_pub.ts:111`
`console.log("published :", message.transcriptText)`). python 쪽은 `translated:`·
`hub: broadcast:` 둘 다 영문뿐이라(`sentenceTranslator.py:31`, `websocket.py:455`)
**python 만 보고 "원문 로깅이 없다" 고 판단하면 틀린다 — 실제로 그렇게 오판했다.**
`docker logs node | grep '^published :'` 로 수집한다.

수집 시 주의 2가지:

- **`transcriptText` 는 문장이 아니라 델타 조각이다.** `InterimChunkOrchestrator` 가
  interim 간 차분(`computeDelta`)을 큐에 쌓아 `join("")` 한 것이라, 고유명사가
  **두 published 줄에 걸쳐 쪼개질 수 있다**("나뮤" + "다"). 세그먼트 단위로 순서대로
  이어 붙여 복원한 뒤 오인식 형태를 뽑을 것
- `normalize()` 가 `.,!?` 를 제거하고 공백을 접은 뒤의 텍스트다 — 원음 그대로가 아니다

**조각화가 오히려 STT 레벨 정규화를 지지하는 근거다.** `transcriptNormalization` 은
구글이 전사문에 적용한 뒤 우리 델타 계산이 돌므로 경계 문제가 없다. 반대로 우리 코드에서
사후 치환하면 조각에 걸쳐 잘린 단어를 못 잡는다.

**watch-service tick 에는 안 보인다** — 스크립트의 node grep 패턴에
`published` 가 없다. 예배 중 원문을 보려면 tick 이 아니라 위 명령을 따로 쓸 것.

**배포하면 로그가 통째로 사라진다 (2026-08-17 실측).** 8/16 예배 원문을 회수하려
했으나 **0줄이었다** — 오늘 10:31 배포가 컨테이너를 재생성해(§5 "전 컨테이너
재생성") 이전 컨테이너의 로그가 함께 삭제됐다. `docker logs` 는 **현재 컨테이너**
것만 보여준다. 로테이션(10m×3)에 밀려 사라지는 것과는 **다른 경로**다.

→ **로그 기반 데이터는 배포 전에 회수해야 한다.** 8/19 예배 원문은 그날 예배가
끝난 뒤·다음 배포 전에 파일로 내려받을 것:
`ssh ... "docker logs node 2>&1 | grep '^published :'" > 8-19-source.txt`

**과보정 위험을 반드시 대조군으로 검증할 것.** 문서가 명시한 리스크이고, 한국어에서
특히 "아멘↔아무/어머", "다윗↔다음", "이삭↔이사" 가 위험하다. 검증은 고유명사 밀집
구간 5~10분 골든셋으로 **adaptation on/off A/B** — 타깃 재현율↑ 뿐 아니라 **오삽입
수**와 **목록 밖 구간 WER**, **final latency 분포**를 함께 볼 것. 통과만 보면
"넣었더니 좋아졌다" 는 착시가 난다. phrase set 초안(48개, 3티어)과 boost 이진 탐색
절차는 이 세션 대화에 있다.

### WS 만료 close code 계약 (PR #100 → develop, 미배포)

클라이언트가 재연결 예산을 **"서버가 끊김 후 300초 pending 큐 유지"** 전제로 잡고
있었는데 **그 큐는 P1(#81)에서 제거됐다.** 시간으로 추측할 근거가 없어 서버가
close code 로 알려주는 쪽으로 정비했다. 계약 표는 §2-1.

정비 내용 3건: `CLOSE_SESSION_ENDED(4410)` 신설(detach 경로 한정), 업링크
접속 시점 검증(기존엔 첫 오디오 프레임이 와야 거절), `/api/mic/stop` 의
`ended` 통과. 리뷰가 4건을 잡았고 그중 **medium 1건은 이 변경이 새로 만든
프로세스 사망 경로**였다 — 거절 분기가 `socket.on("error")` 등록보다 앞서
`return` 해서, 거절된 소켓이 프로토콜 위반 프레임을 하나 더 보내면 uncaught
`'error'` 로 전 세션이 함께 끊길 수 있었다(`crashGuards.test.ts` 가 P1 으로
못박은 부류). 검증: node 141 passed / python 189 passed, 음성 대조군 2회.

### 판단검증 단계 도입 (개인 설정 — 리포 밖)

**구현물에는 게이트가 있고 판단물에는 없었다**는 격차를 메웠다. 코드는 테스트·
리뷰·`$TEST_CMD` 를 통과하는데 *주장*(원인 판정·우선순위·"X 가 없다")은 아무
검사 없이 구현으로 흘러갔다. `serial-agent-pipeline` 에 설계 다음 단계로
`judge` 를 넣어, 설계의 하중받는 주장에 근거 등급(실측/코드확인/추정)을 매기고
확인 가능한 것을 저장소에서 직접 재현하게 했다. 반박·미확인이 0 이 아니면
**`AUTO=1` 이어도 멈춘다.**

전역 `CLAUDE.md` 에 "주장의 근거" 절도 신설했다 — 기존 규칙("확인할 수 **없으면**
질문")이 이 세션에서 반복된 오류(확인이 **가능한데** 안 하고 단정)를 덮지 못했다.

**실전 1회 결과**: judge 가 설계의 틀린 근거를 실제로 잡았다(설계가 "두 게이지의
시딩 시점이 다르다"를 근거로 썼는데 `StreamOrchestrator.ts:73-74` 를 열어 인접
시딩임을 확인해 반박). 산출물은 `docs/session-pause-detail-{plan,judge}.md`.
같은 실행에서 파이프라인 결함 1건도 드러났다 — `set -e` 때문에 tty 없는 환경
(cron·백그라운드)에서 사람 게이트가 `die` 를 건너뛰고 exit 1 로 죽어, 호출자가
"게이트에서 막힘"을 다른 실패와 구분할 수 없었다. `|| ans=n` 가드로 해소.

### 비용 실측 — "TTS 남용이 1순위" 는 틀린 판정이었다

이 세션 중반에 `/api/mic/tts` 무인증을 1순위 비용 위험으로 올렸는데 **실측이
그걸 뒤집었다.** Cloud Monitoring 직접 조회 결과:

| | TTS | STT |
|---|---|---|
| 30일 요청 | 590건 (전부 Standard 음성) | **285,133 스트리밍 요청** |
| 무료 한도 | 월 400만 자 — 사용량 약 10% | **월 60분 오디오** |

**TTS 요금은 사실상 0 이고, 실제 비용 항목은 STT 다.** 어제 예배 44분으로 STT
무료 한도를 거의 다 쓰므로 이건 남용이 아니라 **정상 운영비**다. 백로그에서
"TTS 남용"이 아니라 **"감지 수단(예산 알림) 부재"** 로 볼 것 — 사용자 판단으로
지금은 보류(급하지 않음). 레이트 제한은 정상 사용을 안 깨는 사이징에서 비용
방어가 안 되고(5r/s×500자 = 하루 2억 자) 업링크 WS 와 경로가 겹쳐 재연결을
막을 위험이 있어 **기각**. 길이 상한과 GCP 쿼터·예산 알림이 실효 있는 수단이다.

### 핸드오프 이중화 해소 — `.claude/handoff.md` 삭제 (2026-08-18)

**`.claude/handoff.md` 가 8/9 하루만 쓰이고 방치된 로컬 사본으로 남아 있었다.**
`.gitignore:44` 의 `.claude/` 때문에 **한 번도 추적된 적이 없고**(전 브랜치·삭제분
포함 `git log --all --full-history` 0건), 8/10 이후 커밋 20여 개(#87~#101)가 전부
이 문서에만 반영됐다. 그런데도 파일이 남아 있어 **세션이 낡은 쪽을 먼저 읽는 사고가
실제로 났다** — 그 파일의 "다음 할 일 1번" 은 #86 에서 기각된 유튜브 수집이다.

전역 `CLAUDE.md` 의 핸드오프 절이 경로를 `.claude/handoff.md` 로 못박고 있던 것이
원인으로 보여, **리포별 정본 표**로 바꿨다(이 리포는 이 문서 + `handover-archive.md`).

삭제 전에 619줄을 전수 대조했다. 대부분은 **PR 본문에 더 두껍게 남아 있었다** —
P1 리뷰 4건·e2e 결과·커밋 분리 예외는 PR #81, OBS 실측 6건·기각 대안 2건·뒤집은
테스트 결정은 PR #83 과 커밋 `f8d5b77` 본문이 원본이다. 리포 어디에도 없던 5건만
옮겼다: e2e 재현 방법(→ `multi-listener-p1-plan.md` §6), dev 스택 함정 3건(→ §5),
blip 해석 근사치 2건(→ `multi-listener-p1-plan.md` §2 · `session-lifecycle-plan.md` §9),
사용자 결정 4건 결론(→ §3), P3 skip 테스트 개수 정정(4→5).

# /monitor 페이지 v3 — 자막 스크롤 UX + 도메인 분리 계획 (세션 핸드오프 문서)

> 이 문서는 여러 Claude 세션에 걸친 작업의 단일 연결 고리다.
> **모든 세션은 시작 시 이 문서 전체를 읽고, 자기 작업 단위가 끝나면
> §6 상태 테이블과 §7 세션 로그를 갱신한 뒤 종료한다.**

작성: 2026-08-02 · 발주자: jinseong

`docs/monitor-page-v2-plan.md`(WU1~WU5)의 **연장이 아니다.** v2 는 실시간
모니터링 기능 자체를 만드는 계획이고 WU5 로 닫힌다. 이 문서는 그 범위 밖에서
새로 들어온 두 건을 다룬다.

**선행 조건**: v2 의 WU5 머지 + WU-E 통합 리뷰 + release 배포가 끝난 뒤에
착수한다. prod 가 release #57(WU1까지)에 멈춰 있는 상태에서 프런트를 건드리면
"고친 것"과 "배포되면 어차피 달라지는 것"이 섞여 검증이 불가능하다.

---

## 1. 배경 / 문제 정의

### 1-1. 자막 자동 스크롤이 기대대로 동작하지 않는다 (사용자 제보)

2026-08-02 사용자 제보 — prod `https://neemba.app/monitor/` 에서:
- 라이브 세션을 보는 중 새 번역이 도착해도 화면이 따라가지 않는다
- 세션 상세에 처음 들어갔을 때 맨 아래(최신)로 가지 않는다

### 1-2. 모니터 페이지를 별도 도메인으로 분리하고 싶다

`neemba.app/monitor` → `monitor.neemba.app`. 운영/관측 표면을 서비스 도메인과
분리하는 일반적인 관례를 따르려는 것.

**분리로 얻는 것과 못 얻는 것을 구분해 둔다** (2026-08-02 확인):

| 기대 | 실제 |
|------|------|
| 접근 통제 경계가 도메인 단위로 깔끔해짐 | ✅ 가장 큰 실익 |
| nginx location 우선순위 다툼 해소 | ✅ 현재 `/api/monitor/` 를 `/api/` 보다 먼저 매칭시키려 신경 쓰는 비용이 사라짐 |
| 액세스 로그에서 운영자/사용자 트래픽 분리 | ✅ 스캐너 트래픽에 실사용이 묻히는 문제 완화 |
| 쿠키·자격증명 격리 | △ 현재 Basic Auth 라 쿠키 없음 |
| **장애 독립성** | ❌ **얻지 못한다.** 같은 nginx·같은 EC2 를 탄다. 앱이 죽으면 같이 죽는다 |

마지막 줄이 중요하다. "앱이 죽어도 살아 있어야 하는" 것은 **status page**
(공개용, 별 인프라·별 호스팅)의 논리다. `/monitor` 는 인증 뒤의 **internal
monitoring dashboard** 이므로 그 이점은 해당되지 않는다. 도메인 분리의 근거는
접근 통제와 라우팅 정리로 충분하며, 그 이상을 기대하면 안 된다.

## 2. 확정된 결정 사항 (2026-08-02 사용자 승인)

| # | 결정 | 내용 |
|---|------|------|
| V1 | 스크롤 동작 | **맨 아래에 있을 때만 따라간다.** 사용자가 위로 올려 이전 내용을 읽는 중이면 자동 스크롤 정지 + "새 번역 N건" 버튼 노출 |
| V2 | 진입 시 | 세션 상세 진입 시 맨 아래(최신)로 이동 |
| V3 | 도메인 범위 | `/monitor`·`/api/monitor/`·`/ws/monitor*` **3개 전부** `monitor.neemba.app` 으로 이전 |
| V4 | 기존 경로 | `neemba.app/monitor` 는 **301 리다이렉트로 유지** (제거하지 않음) |
| V5 | 인증서 | 기존 인증서에 **SAN 추가** (별도 인증서 발급 안 함) |
| V6 | Cloudflare | **프록시 켜지 않는다(DNS-only 유지).** Basic Auth 그대로. §5-B 는 후보로만 기록 |
| V7 | 범위 | 이번 문서화 시점에는 **문서만**. 구현은 별도 세션 |

**V6 의 근거**: 도메인 분리와 프록시 전환을 한 번에 넣으면 문제 발생 시 원인
분리가 불가능하다. 특히 프록시는 WebSocket 유휴 타임아웃을 건드려 모니터
페이지가 주기적으로 끊길 수 있는데, 그게 도메인 때문인지 프록시 때문인지
가릴 수 없게 된다. A → B 순서로 간다.

불변 제약 (v2 에서 승계):
- 렌더링은 `textContent` 전용 (innerHTML 금지)
- 모니터 노출 텍스트는 masked 유지
- 모니터 표면 3개 전부 Basic Auth 유지
- 경보 **판정**은 사이드카 책임 — 페이지는 표시만

## 3. 조사 결과 (2026-08-02 실측)

### 3-1. 자동 스크롤 — 코드는 존재한다. 증상 원인은 미확정

**prod `app.js`** (release #57, 14,597 bytes, 2026-07-26 15:53 빌드) — 호출부 1곳:

```js
if (msg.type === "translation") {
  var body = $("detail-rows");
  ...
  body.appendChild(pairRow(msg, { flash: true }));
  body.parentNode.scrollTop = body.parentNode.scrollHeight;   // 라이브 수신 시
}
```

**develop `src/app.ts`** — 호출부 2곳:
- `:594` 라이브 메시지 수신 시
- `:641` gap fill 이력 동기화 루프 안 (WU4 에서 추가)

스크롤 컨테이너는 `.table-wrap { max-height: 70vh; overflow: auto; }` (`style.css:125`).

**따라서 판정은 셋으로 갈린다:**

| 증상 | 코드 상태 | 분류 |
|------|-----------|------|
| 라이브 중 새 번역이 와도 안 따라감 | prod·develop **둘 다 코드 있음** | **원인 미확정** — 아래 가설 참조 |
| 진입 시 맨 아래로 안 감 | **어디에도 코드 없음** | 신규 기능 |
| 맨 아래일 때만 따라가기 + "새 번역 N건" | **어디에도 코드 없음** | 신규 기능 |

> **2026-08-02 갱신 — 위 표의 "prod" 열은 이제 낡았다.**
> release #65·#66·#68 로 WU2·3·4·5 와 통합 리뷰 수정이 전부 prod 에 올라갔다
> (`4dfe434`). 즉 prod `app.js` 는 더 이상 14,597 bytes 짜리 옛 빌드가 아니라
> `src/app.ts` 빌드 산출물이고, 스크롤 호출부도 **2곳**(라이브 수신 + gap fill)이다.
> 아래 가설 1 의 전제("prod 는 WU4 미배포")가 사라졌으므로 **이제 반증이 가능하다.**
> 덧붙여 통합 리뷰에서 세션 목록 30초 병합 폴링이 추가됐다 — 스크롤 컨테이너를
> 건드리지 않지만 목록 행이 주기적으로 재렌더되므로 조사 시 염두에 둘 것.

**첫 줄의 가설 (착수 세션에서 먼저 반증할 것)**:
1. **라이브가 켜져 있지 않았다** — 제보 시점의 prod 는 WU4 미배포라
   **자동 구독(D5)이 없었다.** live 세션을 열어도 "라이브 시작" 을 누르지 않으면
   새 행이 도착하지 않고, 도착하지 않으면 스크롤할 것도 없다. **가장 유력하다.**
   → 2026-08-02 배포로 자동 구독이 들어갔으므로, **같은 화면에서 재현되지 않으면
   이 가설이 확정**된다(버그가 아니라 미배포였다는 결론).
2. 스크롤 대상 오지정 — `body.parentNode` 가 실제 스크롤 컨테이너가 아니어서
   `scrollTop` 대입이 무시된다.
3. `appendChild` 직후 레이아웃 확정 전에 `scrollHeight` 를 읽어 한 행 모자란다.

**반증 방법**: release 배포 후(WU4 포함) 같은 화면에서 재현되는지 먼저 본다.
재현되지 않으면 가설 1 확정 — 버그가 아니라 미배포였다. 재현되면 2·3 을
DevTools 로 좁힌다. ~~**배포 전에 프런트를 고치지 말 것.**~~
→ **2026-08-02: 배포 완료. 이 제약은 해제됐고 반증을 바로 실행할 수 있다.**

**반증 절차 (구체)**:
1. `https://neemba.app/monitor` 를 연다. 슬래시 없이 들어가도 이제 `/monitor/` 로
   301 된다(release #68). **첫 진입은 하드 리프레시**(Cmd+Shift+R) — 옛 캐시가
   남아 있으면 옛 `app.js` 를 보게 된다(2026-08-02 실제로 겪은 함정).
2. live 세션이 있으면 클릭한다. **D5 자동 구독이라 "라이브 시작" 을 누르지 않아도
   수신이 시작돼야 한다.** 모드 라벨이 "라이브" 로 바뀌는지 확인.
3. 새 번역이 도착할 때 목록이 따라 내려가는지 본다.
   - 따라가면 → **가설 1 확정**. 남는 일은 신규 기능(V1·V2)뿐이다
   - 안 따라가면 → 가설 2·3. DevTools 로 `.table-wrap` 이 실제 스크롤 컨테이너인지
     (`scrollHeight > clientHeight` 인지) 확인하고, `scrollTop` 대입 직후 값이
     유지되는지 본다
4. 예배 시간이 아니라 live 세션이 없으면 dev 로 재현한다. 하니스는
   monitor-page-v2 §7 WU3 세션 메모의 socat 브리지 방식이 그대로 쓰인다
   (2026-08-02 통합 리뷰 세션에서도 이 방식으로 검증했다).

### 3-2. 도메인 — DNS·인증서 현황

```
neemba.app      A → 13.125.26.93      (EC2 직결)
www.neemba.app  A → 13.125.26.93
monitor.neemba.app → 없음
NS → kara.ns.cloudflare.com / pete.ns.cloudflare.com
인증서 SAN → neemba.app, www.neemba.app        (monitor 없음)
```

**Cloudflare 는 DNS 만 하고 프록시는 꺼져 있다(회색 구름).** 근거 2가지:
A 레코드가 Cloudflare anycast(104.x·172.67.x)가 아니라 EC2 실 IP 를 반환하고,
nginx 액세스 로그에 클라이언트 실 IP 가 `$remote_addr` 로 찍힌다.

**프런트는 수정 불필요** (develop 기준):

```ts
// src/app.ts:171-172
const proto = location.protocol === "https:" ? "wss:" : "ws:";
return `${proto}//${location.host}${path}`;
```

`location.host` 기반이고 API 도 `const API = "/api/monitor"` 상대경로(`:115`)라
`monitor.neemba.app` 에서 열면 자동으로 따라간다. **단 이건 develop 코드
기준이며, prod 의 구버전 `app.js` 도 동일한지는 배포 시점에 확인해야 한다.**

## 4. 작업 단위 (WU)

### V3-A — [프런트] 자막 스크롤 UX **(release 배포 이후 착수)**

**선행**: v2 release 가 prod 에 배포되어 WU4(자동 구독·gap fill)가 살아 있을 것.

1. §3-1 의 가설 1 을 먼저 반증한다. 배포 후에도 재현되는지 확인.
2. 스크롤 상태 판정 도입 — 컨테이너가 바닥 근처인지:
   `scrollHeight - scrollTop - clientHeight <= THRESHOLD` (THRESHOLD 는 여유값,
   예: 32px. 정확히 0 비교는 소수점 스크롤·확대 배율에서 깨진다)
3. 바닥이면 기존처럼 따라가고, 아니면 스크롤하지 않는다 (V1)
4. 따라가지 않은 동안 도착한 건수를 세어 "새 번역 N건" 버튼 노출.
   클릭 시 맨 아래로 이동 + 카운터 리셋
5. 세션 상세 진입 시 맨 아래로 이동 (V2) — 초기 이력 로드 완료 **후**
6. gap fill(`:641`) 경로도 같은 판정을 타게 한다. 지금은 무조건 점프라,
   사용자가 위를 읽는 중에 재연결이 일어나면 위치를 뺏긴다

**함정**:
- `appendChild` 직후 `scrollHeight` 는 레이아웃 확정 전 값일 수 있다.
  스크롤을 다음 프레임(`requestAnimationFrame`)으로 미루는 편이 안전하다
- 자동 스크롤이 일으킨 `scroll` 이벤트를 사용자 스크롤로 오인하면
  "따라가다 말다" 가 된다. 프로그램적 스크롤 중임을 표시하는 플래그 필요
- 세션 전환 시 카운터·플래그 리셋 (v2 의 epoch 가드 패턴 참고)

**완료 기준**: 바닥에서 수신 → 따라감 / 위로 올린 상태에서 수신 → 안 따라가고
버튼에 건수 누적 / 버튼 클릭 → 맨 아래 + 리셋 / 상세 진입 → 맨 아래 /
재연결 gap fill 중에도 위 규칙 유지.

### V3-B — [인프라] 모니터 도메인 분리 (A안) **(코드 작업 적음, 순서 중요)**

**실행 순서** — 이 순서를 지켜야 중간에 502/인증서 오류가 안 난다:

1. **Cloudflare DNS**: `monitor.neemba.app` A → `13.125.26.93`,
   **회색 구름(DNS-only) 유지** (V6)
2. **인증서 SAN 추가**: 기존 인증서를 확장한다. certbot webroot 방식 유지 —
   `-d neemba.app -d www.neemba.app -d monitor.neemba.app`.
   **DNS 전파 후에 실행할 것** (http-01 챌린지가 새 이름으로 접속해야 함)
3. **nginx**: `monitor.neemba.app` server 블록 신설, 모니터 3경로 이관.
   기존 `neemba.app` 의 `/monitor` 는 301 리다이렉트로 교체 (V4).
   `/api/monitor/` 를 `/api/` 보다 먼저 매칭시키던 주석·순서 의존은
   분리 후 불필요해지므로 정리
4. **nginx reload** 후 확인: `https://monitor.neemba.app/monitor` 200,
   Basic Auth 챌린지 동작, `/ws/monitor` 핸드셰이크 성공,
   `https://neemba.app/monitor` → 301
5. 프런트 확인 — `location.host` 기반이라 무변경 예상이나 **실제 화면에서
   WS 연결 성공을 눈으로 확인**할 것 (§3-2 단서 참조)

**함정**:
- 2번을 1번 전에 하면 챌린지 실패. **DNS 먼저**
- nginx 는 12시간마다 self-reload 로 갱신된 인증서를 집는다. 수동 배포 시엔
  `nginx -s reload` 를 직접 쳐야 즉시 반영된다
- Basic Auth htpasswd 경로(`/var/lib/neemba/secrets/monitor.htpasswd`)는
  새 server 블록에도 그대로 마운트되어야 한다 (compose 볼륨 이미 존재)

**완료 기준**: 위 4·5번 확인 항목 전부 통과 + certbot `renew --dry-run` 성공.

### V3-C — (후보, 이번 범위 밖) Cloudflare 프록시 + Access

§5-B 참조. **V3-B 완료 후에만 검토한다.**

## 5. 열린 질문 / 후보

### 5-A. 열린 질문

1. "새 번역 N건" 버튼의 위치·형태 — 목록 하단 고정 배너인지, 우하단 플로팅인지.
   구현 세션에서 사용자 확인.
2. `THRESHOLD` 값 (바닥 판정 여유). 32px 제안이나 실화면에서 조정 필요.

### 5-B. Cloudflare 프록시 전환 (B안, 후보)

프록시를 켜면 **Cloudflare Access** 로 Basic Auth 를 SSO 로 대체할 수 있다.
서브도메인 분리의 최대 실익이지만, 선행 해결 3건이 있다:

1. **WebSocket 유휴 타임아웃** — 무료 플랜 기준 100초. 현재 nginx 는
   `/ws/monitor` 에 `proxy_read_timeout 3600s` 를 주고 있어 정책이 충돌한다.
   모니터 라이브가 주기적으로 끊길 수 있음 (v2 의 자동 재연결이 흡수하겠지만
   연결 churn 이 상시화된다)
2. **클라이언트 실 IP** — `$remote_addr` 가 Cloudflare IP 가 되므로
   `CF-Connecting-IP` 처리 필요. 액세스 로그·`X-Real-IP` 전달 경로 수정
3. **SSL 모드** — Cloudflare 측이 반드시 **Full (strict)**. Flexible 이면
   nginx 의 http→https 리다이렉트와 물려 무한 리다이렉트가 난다

## 6. 진행 상태

| WU | 상태 | 세션 일자 | 비고 |
|----|------|----------|------|
| V3-A 스크롤 UX | **착수 가능 (선행 조건 충족)** | — | v2 release 가 2026-08-02 prod 배포 완료(`4dfe434`) → 선행 조건 해소. **§3-1 가설 1 반증이 첫 단계, 코드보다 재현이 먼저** |
| V3-B 도메인 분리 | 미착수 | — | 급하지 않음. 순서 의존이 강해(DNS → 인증서 SAN → nginx → reload) 차분한 세션 필요 |
| V3-C 프록시+Access | 후보 | — | V3-B 완료 후 검토 |

## 7. 세션 로그

- 2026-08-02 (주일예배 prod 모니터링 세션): 본 문서 작성. 코드 변경 없음.
  사용자 인터뷰로 V1~V7 확정. 자동 스크롤은 prod·develop 코드를 실제로 대조해
  "미배포 / 신규 기능 / 원인 미확정" 셋으로 분류했다 — **버그로 단정하지 않은
  것이 이 조사의 결론**이다. 도메인은 DNS·인증서 현황을 실측해 A안 절차를
  확정했다. 다음 세션: v2 release 배포 완료 후 `neemba/docs/monitor-page-v3-plan.md
  읽고 V3-B 진행해` (V3-A 는 배포 후 재현 확인이 먼저).
- 2026-08-02 (v2 통합 리뷰·release 세션): **v3 의 선행 조건이 해소됐다.** 코드
  변경은 v2 쪽뿐이고 v3 문서는 사실 갱신만 했다.
  **prod 가 `4dfe434` 로 올라갔다** — WU1~WU5 + 통합 리뷰 수정 + 정적 자산 수정.
  release 3회(#65 → #66 → #68). 자세한 내역은 `monitor-page-v2-plan.md` §6·§7.
  **v3 에 영향 있는 변화 3가지**:
  ① §3-1 표의 "prod" 열이 낡았다 — prod `app.js` 는 이제 `src/app.ts` 빌드
  산출물이고 스크롤 호출부가 2곳이다. 가설 1 의 전제가 사라져 **반증이 가능해졌다**
  (§3-1 에 반증 절차를 구체적으로 적어 뒀다).
  ② 세션 목록에 **30초 병합 폴링**이 추가됐다(통합 리뷰에서 WU5 의 "결정 3:
  목록 폴링 없음" 을 뒤집음). 스크롤 컨테이너는 안 건드리지만 목록 행이 주기적으로
  재렌더되므로 V3-A 조사 시 염두에 둘 것.
  ③ `location = /monitor { absolute_redirect off; return 301 /monitor/; }` 가
  들어갔다. **V3-B 3단계("기존 `/monitor` 는 301 리다이렉트로 교체")의 자리를 미리
  만들어 둔 셈**이라 중복 작업이 아니다 — 도메인 분리 때 이 블록의 목적지만 바꾸면 된다.
  **함정(실제로 겪음)**: 배포해도 화면이 안 바뀌면 캐시를 의심할 것. release #68 로
  `Cache-Control: no-cache` 를 넣었지만, **그 전에 캐시된 index.html 을 들고 있는
  브라우저는 한 번 하드 리프레시가 필요하다.**
  **미해결 이월(v2 §7 에 근거 기록)**: 세션 목록 상관 서브쿼리 인덱스(prod 실측 후
  판단 — 사용자 결정), STALE 판정의 서버/브라우저 시계 스큐, WU5 코드 리뷰 잔여 8건
  (전부 표시 계층 열화라 실제로 아픈 것만 골라 처리하기로 함).
  **우선순위 주의**: 이 세션 끝에 `handover-2026-08-02.md` §2 큐를 재정렬했다.
  **V3-A 는 2순위이고 1순위는 WU-F(`session-lifecycle-plan.md` §11)** 다.
  F-1(`ws_blips` pending 행 미종료)이 예배를 치를 때마다 데이터를 더 오염시키고,
  release #66 으로 방금 띄운 **순단 이력 탭이 지금 그 오염된 데이터를 보여준다.**
  V3-A 는 가설 1 이 맞으면 반증만으로 닫히므로 급하지 않다.
  V3-A 를 먼저 할 경우의 프롬프트: `neemba/docs/monitor-page-v3-plan.md 읽고
  V3-A 진행해` — **코드보다 §3-1 반증 절차가 먼저다.**

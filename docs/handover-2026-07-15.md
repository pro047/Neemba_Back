# 인수인계 문서 (2026-07-15)

> `docs/handover-2026-07-12.md`의 후속. 그 문서 §4(CI 게이트)·§5(운영 세팅)를 실행한 세션의 결과와 다음 작업 정리.
> 자동 recall 요약: `~/.claude/projects/-Users-ijinseong-Documents-translation-neemba/memory/neemba-workflow-and-backlog.md`

---

## 1. 작업 규칙 (변함없음)

- **브랜치**: `main - develop - feature`. 모든 PR은 develop으로, develop→main은 release PR로만. feature 브랜치는 머지 후 삭제.
- **TDD**: red → 수정 → green + 전체 스위트 + `tsc --noEmit`/`py_compile` → 한국어 conventional commit → PR → 머지.
- **테스트**: Node `cd services/node && npm test && npx tsc --noEmit` (**58 passed**로 증가). Python `cd services/python && uv run --extra dev pytest tests/ -q` (**83 passed**).
- **gh 계정**: `pro047`.
- **신규(2026-07-15)**: ① develop/main에 branch protection — `node-test`·`python-test` 필수 체크(잡 이름 바꾸면 protection 설정도 같이 갱신, 불일치 시 머지 영구 블록) ② **AWS 변경은 `infra/terraform/`에서 코드→plan 검토→apply로만. aws cli 직접 변경 금지.**

## 2. 이번 세션(2026-07-15) 완료 요약

| 작업 | PR | 비고 |
|---|---|---|
| CI 테스트 게이트(B): deploy.yml `test` 잡 + `needs: test` / ci.yml 신설 | #28 | 테스트 실패 시 배포 잡 미시작(SG 부작용 원천 차단) |
| ↳ 게이트가 첫 실행에서 잠복 결함 검출: TTS auth client eager `getClient()` → CI에서 unhandled rejection | #28 내 | lazy화(`clientPromise ??= auth.getClient()`)로 수정 |
| branch protection(C): develop/main 필수 체크 | (리포 설정) | B(배포 차단)+C(머지 차단) 이중 방어선 |
| 인증서 D-14 감시: cert-watch.yml 일일 cron(06:00 KST) | #29, release #30 | 14일 미만 시 실패 메일. 수동 dispatch로 검증(잔여 86일) |
| Postgres 백업: pg-backup 사이드카(pg_dump -Fc→S3, 90일 lifecycle) | #31, release #32 | 자격증명은 EC2 instance profile(IMDS) — env에 키 0개. **복원 리허설 완료**(실덤프→일회용 pg→행 수 확인) |
| AWS 테라폼 코드화: 백업 자원 6개 import 흡수 | #33 | plan "6 to import, 0 to change" → drift 0. state=`s3://hymn-tfstate/neemba/terraform.tfstate` |
| RTMP 하드닝: ffmpeg 지수 백오프(10s→cap 60s, 포기 없음)·`RTMP_PULL_URL` env화(prod 내부망)·on_publish 스트림 키 인증 | #34 | 인증은 `RTMP_PUBLISH_KEY` 미설정 시 꺼짐(2단계 전환). 테스트 +12 |
| release(테라폼·RTMP 포함 develop→main) | #35 | 이 문서와 함께 배포 |

### 세션 중 발견한 사실 (다음 세션이 알아야 할 것)

- **서버 실 IP = 13.125.26.93** (EIP, `neemba` 인스턴스 i-067ebdc3337b14ff9). 이전 문서의 43.202.219.217은 구값 — SSH alias 확인 필요.
- **nginx-rtmp는 `on_publish` 호스트를 설정 파싱 시점에 해석** → rtmp 서비스에 `depends_on: node` 필수(없으면 선기동 시 emerg 크래시루프). PR #20 dev nginx stale upstream과 같은 계열.
- **생성자에서 promise를 eager 시작하고 즉시 관측하지 않으면 unhandled rejection 폭탄** — 로컬(자격증명 있음)에선 안 터지고 CI에서만 터짐. 비슷한 외부 클라이언트 생성부 작성 시 lazy로.
- 로컬 `ljs` IAM user는 **AdministratorAccess** — 에이전트 명령 승인 = admin 실행. 완화책이 테라폼 전환(plan diff 게이트)이었고, 최소 권한 분리는 백로그(§3-5).
- deploy run에 actions/*@v4의 Node20 deprecation 경고(당장 무해).

## 3. 다음 작업 (우선순위 순)

### 3-1. UptimeRobot `/health` 감시 — **사용자 작업 (~5분, 빠를수록 좋음)**
켜기 전까진 서비스 다운을 알 방법이 없음. uptimerobot.com 가입(무료) → New monitor → HTTP(s) → URL `https://neemba.app/health`(루트나 `/monitor` 아님 — 401 오탐) → 5분 간격 → 이메일 알림 확인 → 생성 후 Up(초록) 확인.

### 3-2. RTMP 인증 활성화 — **사용자 작업 (서두를 필요 없음)**
1. 교회 OBS 스트림 키: `translation` → `translation?key=<비밀값>` (서버 주소는 그대로)
2. 로컬 `.env.prod`에 `RTMP_PUBLISH_KEY=<비밀값>` 추가 → `gh secret set ENV_PROD -R pro047/Neemba_Back < .env.prod`
3. 다음 배포부터 강제. 앱은 무변경(RTMP 발행 안 함). **켠 뒤 node 로그에 `auth disabled` 경고가 보이면 env 누락 — 즉시 조치.**

### 3-3. backpressure 부재 (P1 마지막) — **다음 세션 메인 후보**
NATS 중단·소비 지연 시 span이 설계상 드롭됨. 착수 시 **선택지 표부터**(버퍼링·재시도·유실 허용 범위 트레이드오프) 만들어 사용자 결정 후 구현. consumer ack 시점 이동(큐 적재 후→처리 완료 후)과 얽혀 있어 함께 검토.

### 3-4. /ws 인증 — 앱(Flutter) 수정 필요라 mvp WIP 정리 후.

### 3-5. 에이전트/일상용 최소 권한 IAM 분리 — `ljs`(admin) 상시 사용 탈피. 읽기 전용 + 필요 시 상승 구조. 테라폼으로 만들 것.

### 3-6. 기존 이월 백로그
DeepL 클라이언트 재사용·비동기화 / VAD 기반 rotation / prod 모니터링 스택 / Makefile·ESLint 설정 오류.

### 3-7. 저우선
GHA 액션 버전업(Node20 deprecation) / deploy IAM·보안그룹 테라폼 흡수 / mvp 앱 main-develop 구조 세팅(사용자 WIP 정리 후) / terraform 1.10+ 업그레이드 시 S3 native lock 도입 / `infra/terraform/imports.tf` 삭제(1회성 import 완료됨).

## 4. 미해결·주의사항 (이월)

- **mvp 앱 수정**(토스트 순서+rtmp mounted 가드): `~/Documents/translation/mvp` 워킹트리에 커밋 안 된 채 존재. 사용자가 feature/mic WIP 커밋 시 함께.
- **NATS 비밀번호는 영문자로 시작**(숫자 시작 시 conf 치환 실패). 로컬 `.env.*.bak-*` 정리 대상.
- dev 컨테이너 재기동 시 `docker compose -f docker-compose.dev.yml --env-file .env.dev up -d --force-recreate` 권장.
- prod compose에 `${}` 치환 쓰면 배포 명령에 `--env-file` 필수(이미 반영, 새 스텝 추가 시 유의).
